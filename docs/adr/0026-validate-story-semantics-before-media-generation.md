---
status: accepted
---

# 在媒体生成前同时验收故事结构与叙事语义

冻结故事包不仅要通过确定性的 Schema、引用和章节边界检查，还必须由独立验收器检查个性化程度、跨章因果、故事模板机制、安全边界、事实来源和非模板化程度；验收器只输出结构化报告，不能改写故事。格式错误只允许一次受限格式修复，叙事语义错误只允许全局故事规划器完整重规划一次，第二次仍不合格则阻断全部关键帧和视频生成，避免把结构合法但叙事错误的内容扩散为昂贵媒体任务。
