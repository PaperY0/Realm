# 《藏梦书境》产品需求文档（PRD）

> 文档版本：V2.0（比赛 Demo）  
> 更新日期：2026-07-25  
> 当前状态：开工前最终确认稿  
> 产品阶段：Windows 本机、单实例、真实模型调用 Demo  
> 唯一开放世界：内耗世界

---

## 1. 产品定义

### 1.1 一句话定义

《藏梦书境》是一个把用户本次经历转化为七章个性化童话绘本的互动 Web Demo。用户在内耗世界中自然表达一件难以消化的事，系统以固定的“内耗守门 IP”为主角，根据本次输入生成完整故事、八张连续关键帧和七个章节视频；每章播放约五秒后停住，由用户主动进入下一章。

### 1.2 品牌表达

- 正式名称：**藏梦书境**。
- 品牌副标题：**一个把心事写成童话的世界**。
- 入口文案：**推开门，让心事成为童话。**
- 合书文案：**你合上的每一本书，世界都会记得。**
- “藏”表示收藏与安放，不表示压抑情绪。
- 产品不得与“AI 心理治疗”“心理诊断”或治疗效果承诺并列宣传。

### 1.3 产品定位

产品提供的是情绪表达、童话转译和自我观看的体验，不提供心理疾病诊断、量表评估、治疗方案或现实关系裁决，也不替代心理咨询、医疗服务和现实社会支持。

核心价值不是让 AI 直接分析用户，而是：

1. 用轻松、低压力的方式让用户说出一段经历；
2. 把现实张力转译为有距离感的童话因果；
3. 用连续、可观看的七章动态绘本呈现本次独有故事；
4. 让用户在故事中自行看见关系变化，而不是接收说教或通用安慰。

---

## 2. V0 目标与范围

### 2.1 本次必须证明的事情

V0 只需要在一台 Windows 演示电脑上证明以下闭环真实成立：

```text
进入内耗世界
→ 自然表达与可跳过追问
→ 安全处理与内部故事路由
→ 根据本次输入生成完整七章故事
→ 生成八张边界关键帧
→ 七章视频并行生成
→ 第一章可用后开书
→ 用户逐章观看并主动翻章
→ 合书并保留当前任务状态
```

### 2.2 Demo 运行边界

- 一台 Windows 演示电脑；Chrome 或 Edge 全屏运行。
- 本地浏览器加本地 Node.js 服务端。
- 一次只有一名用户、一个浏览器会话、一本当前绘本。
- 使用真实文本、图像和视频模型调用，不用预生成故事或预生成章节视频冒充本次结果。
- 页面刷新后能够恢复当前绘本的生成任务、已完成媒体和阅读进度。
- 提供一键重置，清除当前书、任务状态和对应本地媒体。

### 2.3 本次明确不做

- 不开放悲伤、焦虑、愤怒、喜悦等其他情绪世界，也不展示多世界选择页。
- 不让用户选择标签卷或回声卷。
- 不做第三套内耗故事卷；无法可靠匹配时进入“暂不适配”。
- 不做注册、登录、账户找回、跨设备同步或多人并发。
- 不做公网部署、域名、TLS、云对象存储、CDN、Redis、消息队列或高可用。
- 不做移动端完整适配；手机和平板只要求页面不崩溃。
- 不做剧情分支、对错选项、自动连续播放或自由 3D 探索。
- 不做 AI 配音、角色对白、口型同步、个性化音乐或视频供应商原声。
- 不做付费、订阅、额度、导出、商业化、运营后台或生产级监控。
- 不做账户级长期记忆、生产级三十天定时清理和正式公开版本的数据运营能力。

---

## 3. 目标用户与设计原则

### 3.1 核心用户

- 主要面向能够正常学习、工作和生活，但会因外界评价、害怕犯错、反复确认或反刍而持续内耗的成年人。
- 喜欢童话、绘本、治愈系 IP、轻叙事互动或具有收藏感的数字内容。
- 不想被直接分析、说教或贴心理标签，希望先用更含蓄的方式整理感受。

### 3.2 设计原则

1. **用户决定节奏，系统负责故事。** 用户可以表达、跳过追问、播放、停留、重播、进入下一章和退出，但不能改变剧情走向。
2. **第一至第七章都个性化。** 个性化不只发生在标题、正文或某个高潮镜头，而是发生在完整故事、边界状态和七个章节媒体上。
3. **隐喻优先于解释。** 不直接复述现实经历，不替用户下结论，不把童话写成建议清单。
4. **因果约束，不固定剧情。** 模板约束故事机制和安全边界，不预先规定七章分别发生什么。
5. **一个规划者拥有故事事实。** 章节 subagent 只能渲染，不能改写故事。
6. **失败要降级，不要伪装。** 网页降级仍然个性化，但内部明确标记为 `dynamic_fallback`。
7. **安全状态描述工作流，不描述用户。** 不使用临床风险等级或人格判断。
8. **Demo 优先于生产设计。** 只实现比赛现场需要真实验证的最小闭环。

---

## 4. 世界、故事卷与路由

### 4.1 产品层级

```text
藏梦书境
└─ 内耗世界（V0 唯一世界）
   ├─ 标签卷（内部路由）
   └─ 回声卷（内部路由）
```

“标签卷”和“回声卷”只存在于内部 `StoryTemplateMatch`、模板版本和调试报告中。页面不出现“进入标签卷”“进入回声卷”或任何心理类型选择。

### 4.2 路由结果

`StoryTemplateMatch.route` 只能是：

- `label`：进入标签模板；
- `echo`：进入回声模板；
- `unsupported`：当前两套模板无法可靠承接，不强行分类，也不启动故事与媒体生成。

“暂不适配”页面使用自然、非诊断式语言说明目前还织不出合适故事，并允许用户重新表达或重置，不展示内部路由理由。

### 4.3 标签卷机制

适用于用户受到外界评价、定义或判断影响，并围绕该判断反复调整、遮掩或证明自己的经历。

必须满足的核心因果：

```text
外界判断以某种童话现象进入故事
→ 守门 IP 的应对让该判断获得更大影响
→ 守门 IP 看见这种放大关系
→ 守门 IP 改变自己与该判断的关系
```

约束：

- 不要求画面中出现实体标签；
- 不固定上述事件分别发生在哪一章；
- 结尾不能靠证明别人完全错误、消灭所有评价或宣布用户完全正确完成；
- 不贬低现实人物，不替用户裁决现实关系。

### 4.4 回声卷机制

适用于用户担心自己犯错、被误解或产生负面后果，并通过检查、确认、回想或反刍试图消除不确定性的经历。

必须满足的核心因果：

```text
担心错误或负面含义
→ 检查、确认、回想或反刍
→ 每次确认产生新的疑点
→ 守门 IP 看见确认如何延长回声
→ 守门 IP 允许部分不确定性，不再无休止确认
```

约束：

- 不要求画面中出现实体回声；
- 不以“停止思考”“保证没有犯错”或旁人保证“你没问题”为结尾；
- 不把谨慎、负责或思考本身描写成缺陷。

---

## 5. 用户体验流程

### 5.1 进入内耗世界

1. 页面以一个封闭、可进入的童话入口开始。
2. 用户通过明确的拖动或点击动作进入；不得用自动播放视频代替关键进入动作。
3. V0 直接进入内耗世界，不展示其他情绪门。
4. 内耗守门 IP 以轻微呼吸、眨眼、视线和线结动作建立存在感，动画服务于引导，不形成长篇对话。

入口体验是固定的世界仪式，不承担个性化故事事实，也不是自由探索游戏。

### 5.2 表达经历

- 守门 IP 在世界内自然邀请用户说出一件仍让自己反复消耗的事。
- 支持文本输入；不要求语音输入。
- 输入界面应像风铃纸条、书页或世界物件，而不是通用客服聊天框。
- 不要求用户先选择情绪标签、故事卷或心理类型。

### 5.3 自然追问

- 最多追问两次，也可以零次。
- 每次只追问一个故事真正缺失的信息。
- 每次都可以跳过；跳过不会被批评，也不会阻断安全的故事生成。
- 文案应轻松、具体、像守门 IP 自然接话，避免问卷式连续提问。
- 前端使用纸条展开、风铃轻动、角色停步等小动画承载追问，不弹出僵硬表单。
- 追问不得诱导用户补充身份信息、诊断信息或模型想象的事实。

### 5.4 安全工作流

用户表达首先进入以下工作流状态：

```text
story_safe
safety_check_required
crisis_route
```

- `story_safe`：可以生成 `SafeStoryBrief` 并继续故事流程。
- `safety_check_required`：暂停童话隐喻与世界动画，用直接、清晰的语言确认用户当前是否安全；只有明确排除当前危险后才返回故事流程。
- `crisis_route`：停止全部故事规划、图像和视频生成，进入现实求助页面。

### 5.5 等待仪式

用户完成表达或跳过最后追问后开始计时：

1. 守门 IP 将安全摘要象征性地折入纸船或书页；
2. 页面逐步显示“故事正在成形”的世界内状态；
3. 可以显示七章生成进度，但不能展示模型名称、供应商日志或技术报错；
4. 不使用空白页加单个旋转图标；
5. 用户不能重复提交同一本书来催促慢任务。

### 5.6 开书条件

满足以下条件后允许打开绘本：

- 完整七章故事规划和故事包已经冻结并通过验收；
- 第一章完整视频已通过验收，或第一章个性化降级版本已就绪；
- 第二章至少有可播放的个性化降级版本；
- 其余章节任务均已启动；
- 第一章可播放资产、正文和播放状态已经写入本地状态。

如果第一章完整视频提前完成，立即允许开书，不为了等待仪式刻意拖满五分钟。

### 5.7 七章阅读

- 一本书固定七章，但七章事件不是固定模板槽位。
- 每章是一个约五秒的连续分镜视频，播放完停在稳定尾帧。
- 视频停止后，本章网页正文继续保留，用户可以按自己的速度阅读。
- 用户点击“下一章”后才进入下一章；不自动连播。
- 章节之间不提供改变剧情的选择。
- 可提供本章重播、静音和退出；不显示浏览器原生视频控件。
- 用户观看期间不得把正在播放的降级版本热切换为迟到的正式视频。

### 5.8 合书与本地恢复

- 第七章结束后，用户主动点击或拖动完成合书。
- Demo 可以用单册书脊、信物或世界变化完成视觉收束，但不需要实现多书运营型书架。
- 页面刷新后恢复当前书、已完成资产、任务状态和阅读进度。
- 一键重置前明确提示不可恢复；确认后删除当前书的 SQLite 记录和对应媒体目录。

---

## 6. 个性化故事生成

### 6.1 生成总流程

```text
用户输入
→ 安全工作流
→ 0～2 次可跳过自然追问
→ SafeStoryBrief
→ StoryTemplateMatch
→ 全局故事规划器
→ StoryBible
→ BoundaryKeyframePlan（K0～K7）
→ 七张 ChapterCard
→ 故事包结构验收
→ 独立叙事语义验收
→ 八张边界关键帧
→ 七个章节 subagent 并行生成视频
→ 自动媒体验收
→ 单次重试或个性化降级
→ 网页绘本编排
```

### 6.2 SafeStoryBrief

`SafeStoryBrief` 是后续所有生成任务可以看到的唯一用户经历来源：

- 只允许在 `story_safe` 状态生成；
- 保留可用于童话转译的抽象事实、核心张力、担心的含义、希望抵达的方向和禁止解释；
- 不包含用户原始输入、完整追问对话、无关身份信息或故事卷路由；
- 明确记录未知信息，未知不等于允许模型补写；
- 只有用户再次明确确认的一句话可以原样进入书中，否则不持久化逐字表达。

### 6.3 故事模板

故事模板由以下部分组成：

```text
适用条件
核心因果机制
宽松叙事锚点
可选灵感
禁止捷径
安全与语言边界
```

模板不包含七个固定 `chapterSlots`。

固定锚点只有：

- 第一章：让守门 IP 进入本次独有故事，并建立核心童话张力；
- 第三至第五章：必须出现因果转折，但具体章节由规划器决定；
- 第七章：守门 IP 与核心判断或不确定性形成新的关系，而不是突然治愈；
- 每一章都必须产生可观察的状态变化，并与前后章节保持因果连续。

场景、事件、象征、动作、节奏和转折表现由全局故事规划器根据本次 `SafeStoryBrief` 自由生成。

### 6.4 全局故事规划器

只有全局故事规划器可以创造或修改故事事实。它负责：

- 文学标题与题记；
- 守门 IP 在本册中的目标、压力和变化；
- 本次独有的世界规则、信物、反复象征和视觉母题；
- 七章功能、因果链、起止状态和用户可见正文；
- K0～K7 八个边界状态；
- 每章约五秒单镜头是否足以表达；
- 禁止发明的事实、语言边界和结尾承诺。

第一至第七章全部依据本次输入生成。不得使用预生成剧情、固定回退故事或只替换标题和道具的故事骨架。

### 6.5 冻结故事包

媒体生成前必须冻结并版本化：

- `SafeStoryBrief`；
- `StoryTemplateMatch`；
- 对应 `StoryTemplate`；
- `StoryBible`；
- `BoundaryKeyframePlan`；
- 七张 `ChapterCard`；
- `StoryPackageValidationReport`。

冻结后章节 subagent 只能读取，不能写回或修改这些事实。

### 6.6 七个章节 subagent

- 七张 `ChapterCard` 分发给七个相互独立的章节生成任务并行执行。
- 每个任务把章节合同转换为图像/视频供应商需要的分镜提示词和首尾帧任务。
- subagent 可以优化景别、一次镜头运动、动作幅度和供应商提示词表达。
- subagent 不得改变守门 IP、场景事实、象征、信物、章节功能、起止状态、正文含义或故事结局。
- 任务无法满足合同时必须返回结构化失败，不得自行改写剧情迁就模型。

---

## 7. 守门 IP 与视觉连续性

### 7.1 主角边界

- 七章主角固定为“内耗守门 IP”。
- 它是故事承载者，不是用户的数字替身。
- 它可以携带用户经历形成的童话张力，但不能声称“我就是你”或替用户解释现实。
- 不为每本书生成新的主角，也不让标签卷和回声卷拥有不同主角。

### 7.2 IP 参考资产

开工前必须准备并人工验收一套固定 IP 参考资产，至少包含：

- 正面、侧面、三分之四角度和背面；
- 身体比例、脸型、服装核心结构、标志性物件和色板；
- 允许变化的表情与姿态范围；
- 禁止变化的角色特征；
- 用于组合关键帧的透明背景素材。

每一张正式边界关键帧都必须重新提供同一套 IP 参考，不允许仅依赖上一张生成图逐帧递推。

### 7.3 八张共享边界关键帧

每本书必须拥有唯一的 `BoundaryKeyframePlan`：

```text
K0 → K1：第一章
K1 → K2：第二章
K2 → K3：第三章
K3 → K4：第四章
K4 → K5：第五章
K5 → K6：第六章
K6 → K7：第七章
```

- 正好八张共享关键帧，不为相邻章节重复生成“看起来差不多”的边界图。
- 第 N 章的开始帧必须引用 K(N-1)，结束帧必须引用 KN。
- 关键帧冻结角色、位置、信物、场景、光色和稳定状态；章内动作由 `ChapterCard` 描述。
- 七个视频任务只有在边界关键帧可用后才能提交。

---

## 8. 章节视频与媒体规格

### 8.1 章节镜头合同

每章必须满足：

- 时长约五秒；
- 一个连续镜头，章内不切镜；
- 一个主要动作；
- 最多一次主要镜头运动；
- 从共享开始关键帧运动到共享结束关键帧；
- 结束时稳定停在尾帧，等待用户进入下一章。

章节正文的阅读时间不受五秒限制。

### 8.2 统一媒体规格

| 项目 | 规格 |
|---|---|
| 画幅 | 16:9 |
| 关键帧 | 1280×720 |
| 章节视频 | 1280×720 |
| 帧率 | 24 fps |
| 容器 | MP4 |
| 视频编码 | H.264 |
| 像素格式 | yuv420p |
| 音轨 | 无 |

- 供应商媒体不得直接发布，服务端使用 FFmpeg 统一标准化后再验收。
- FFmpeg 不得通过破坏性裁切改变首尾帧、安全构图、守门 IP、信物或主要动作。
- 正式视频和网页降级使用相同的 16:9 容器。

### 8.3 桌面视觉目标

- 唯一正式验收环境：Windows + Chrome/Edge 全屏。
- 基准验收视口：1920×1080。
- 1366×768：允许等比缩小，但核心布局、正文和操作必须完整。
- 手机和平板：只要求能够打开且不崩溃，不作为视觉验收目标。
- 守门 IP、信物、主要动作和关键信息必须放在安全构图区内。

---

## 9. 故事包验收

### 9.1 结构验收

确定性检查至少包括：

- 所有对象通过对应 JSON Schema；
- `SafeStoryBrief` 不含原始输入和内部路由；
- 路由、模板和版本引用一致；
- 恰好七张 `ChapterCard`；
- 恰好 K0～K7 八张边界关键帧；
- 相邻章节引用同一个共享边界；
- 每章 `durationSeconds = 5`；
- 每章 `noInternalCuts = true`；
- `unsupported` 不绑定标签或回声模板。

### 9.2 独立叙事语义验收

验收器只输出报告，不能改写故事。至少检查：

- 是否真正使用了本次 `SafeStoryBrief`，而不是通用童话；
- 七章是否存在完整因果链和逐章状态变化；
- 是否符合标签或回声的核心机制；
- 是否过度模板化、只替换名词或重复相同节拍；
- 是否编造用户事实、动机、身份或现实结论；
- 是否出现心理诊断、说教、贬低现实人物或突然治愈；
- 第七章是否形成新的关系，而不是消灭问题；
- 每章是否能够用约五秒单镜头清楚表现。

### 9.3 故事失败规则

- 首次 Schema 格式错误：只允许一次受限格式修复，不改变任何故事事实。
- 首次叙事语义错误：由全局故事规划器完整重规划一次，不能局部打补丁掩盖因果错误。
- 第二次仍不合格：阻断全部关键帧、正式视频和网页降级章节生成，页面显示本次故事未能安全成形并允许重置。

---

## 10. 媒体自动门禁与降级

### 10.1 自动媒体验收

关键帧和章节视频在成为可播放资产前，必须经过：

1. 确定性技术检查：文件存在、可解码、尺寸、帧率、时长、编码、音轨、首尾帧和任务引用；
2. 独立视觉语义检查：守门 IP、场景、信物、主要动作、章节边界、内部切镜、画面崩坏、无关文字、水印和安全内容。

报告结果：

- `pass`：发布；
- `warning`：记录软警告并发布；
- `fail`：执行重试、组合关键帧或网页降级。

比赛等待链路不加入人工逐章审批。可以提供内部调试页查看报告，但验收器不能修改故事事实，也不能只输出一个不可解释的总分。

### 10.2 关键帧失败

- 单张关键帧失败后只重试该张一次。
- 第二次仍失败时，使用预先验收的透明守门 IP、模板场景、本书颜色、信物和象征确定性合成 `composed_keyframe`。
- `composed_keyframe` 不是通用占位图，但不再提交给视频模型。
- 任何以组合关键帧为起点或终点的相邻章节直接使用网页个性化降级；其他章节继续生成正式视频。

### 10.3 正式视频失败

- `queued`、`pending` 和 `processing` 是正常状态，不因为等待时间长而重复提交。
- 只有供应商明确终止失败，或成品未通过自动媒体门禁时，才触发该章唯一一次自动重试。
- 重试可以收紧失败相关提示词，但不能改变冻结故事包。
- 第二次明确失败后使用 `dynamic_fallback`。
- 不设置整本书的视频任务总量上限；七个章节分别拥有自己的重试机会。
- 单章失败不触发整本重规划或重做其他章节。

### 10.4 个性化网页降级

`dynamic_fallback` 使用本章真实边界帧和正文：

```text
起始关键帧
→ CSS / Canvas / WebGL 的缓慢推镜、横移、呼吸、光影或粒子
→ 淡入结束关键帧
→ 稳定停在结束关键帧
```

要求：

- 总时长约五秒；
- 使用与正式视频相同的 16:9 容器、正文和声音层；
- 不调用第二个视频模型；
- 不使用与本次用户经历无关的通用占位片；
- 内部类型必须记录为 `dynamic_fallback`，不得冒充正式 AI 视频。

---

## 11. 生成时限与任务恢复

### 11.1 用户侧五分钟上限

计时起点是用户确认表达完成，或跳过最后一次自然追问。

- 第一章完整视频提前通过验收：立即开书。
- 五分钟时第一章仍未通过：使用已验证的第一章 `dynamic_fallback` 开书。
- 五分钟不表示原视频任务失败，不取消、不重新提交。
- 原视频稍后完成时，只有该章尚未开始观看才可以替换降级版本。
- 用户已开始观看或已经看过该章后，不进行热切换。

### 11.2 后台三十分钟期限

- 每个视频任务从首次提交起最多低频轮询三十分钟。
- 三十分钟内的 `queued`、`pending`、`processing` 均为正常运行。
- 超过三十分钟仍无终止结果：标记为 `stalled` 并停止轮询。
- `stalled` 不触发自动重新提交，本章保留个性化降级。
- 供应商支持取消时尝试取消；不支持时忽略迟到结果。

### 11.3 比赛演示时长

完整比赛演示目标为八至十分钟：

- 进入世界与表达：约一至两分钟；
- 生成等待：最多五分钟后必须能开书；
- 逐章展示、合书和收束：约两至三分钟。

不使用预生成故事或媒体冒充现场输入结果来压缩演示时间。

---

## 12. 声音、文字与播放体验

### 12.1 声音

- 不调用音频模型。
- 不生成 AI 旁白、角色对白、口型或个性化音乐。
- 移除视频供应商返回的所有音轨。
- 使用一条本地预置的轻柔环境音乐，以及少量推门、风铃、翻页和合书 UI 音效。
- 声音只能在用户首次主动开书点击后开始。
- 始终提供可发现的静音开关。
- 声音加载失败或被静音时，用户仍能完整理解故事。

### 12.2 网页正文

- 章节正文由网页 DOM 文字层呈现，不烧录进视频。
- 文字可以随视频渐显，视频停止后继续保留。
- 正文不得遮挡守门 IP、信物、主要动作或安全构图区。
- 不依赖供应商生成正确的中文文字。
- 每章正文应短、具体、有画面感，避免长篇解释影响逐章节奏。

### 12.3 播放控制

- 不显示浏览器原生视频控件。
- 绘本界面统一提供播放、重播、静音、下一章和退出。
- “下一章”只在当前章已到达稳定尾帧后成为主要动作。
- 不用倒计时或自动翻章催促用户。

---

## 13. 隐私、身份与安全数据

### 13.1 匿名身份

- 不实现注册和登录。
- 浏览器生成随机 `visitorId`，不使用可推断真实身份的标识。
- 只承诺在当前浏览器和当前演示电脑恢复任务。
- 清除浏览器数据后不可恢复，不承诺跨设备同步或账户找回。

### 13.2 原始输入

- 正常输入原文最多临时保留三十分钟。
- `SafeStoryBrief` 校验完成后应立即提前删除原文。
- 全局故事规划器、章节 subagent 和模型供应商只能接收 `SafeStoryBrief`，不能接收原始倾诉全文。
- 原始输入不得写入应用日志、章节提示词、媒体验收报告或长期数据库。

### 13.3 危机路径数据

进入 `crisis_route` 后：

- 不创建绘本、信物、故事记忆、关键帧或视频；
- 不持久化用户原始表达；
- 只保存不含原文的最小 `SafetyEvent`，记录触发类别、处置动作、分类器版本和资源包版本；
- 页面切换为直接、清晰、非童话隐喻的现实求助信息。

V0 正式危机资源只覆盖中国大陆，并使用人工维护、带核验日期的资源包；当前指定的统一心理援助热线参考为 12356。资源内容不得由模型临时编造，上线演示前必须再次人工核验国家卫生健康委员会官方信息：

`https://www.nhc.gov.cn/yzygj/c100068/202412/49a1a65386cd4be582d4702fd0926ee8.shtml`

对不在中国大陆的用户，只提供联系当地紧急服务、可信任的人或附近医疗机构等不含具体号码的通用提示。

### 13.4 删除边界

- Demo 的一键重置不可恢复地删除当前书数据库记录和对应本地媒体目录。
- 不保留能够重建用户经历或绘本的软删除副本。
- 未来正式产品保留“单册忘记”和“重置整个世界”的不可恢复删除语义，但不在本次实现完整运营流程。

---

## 14. 本机技术架构

### 14.1 组件

```text
Chrome / Edge Web 客户端
        ↓ 本机 HTTP
本地 Node.js 服务端
├─ 安全与文本工作流
├─ 故事规划与 Schema 校验
├─ 图像/视频任务编排
├─ FFmpeg 标准化
├─ 自动故事与媒体验收
├─ SQLite 状态库
└─ 按 bookId 隔离的本地媒体目录
        ↓ HTTPS
统一中转站
├─ 文本模型
├─ 图像模型
└─ 视频模型
```

### 14.2 状态存储

SQLite 保存：

- 当前绘本和阅读进度；
- `SafeStoryBrief` 及冻结故事包；
- 关键帧、视频、验收、重试、降级和轮询状态；
- 供应商任务 ID 与必要元数据；
- 相对媒体路径；
- 不含危机原文的 `SafetyEvent`。

本地目录保存：

- K0～K7 关键帧；
- 标准化章节视频；
- 组合关键帧与临时转码文件；
- 不把图片和视频保存成 SQLite BLOB。

七个章节任务独立更新自己的记录，避免共同覆盖一个 JSON 状态文件。

### 14.3 建议任务状态

```text
created
→ safety_processing
→ followup_or_briefing
→ story_planning
→ story_validating
→ keyframes_generating
→ chapters_generating
→ partially_ready
→ ready
```

分支状态：

```text
unsupported
crisis_route
story_blocked
retrying
fallback_ready
stalled
reset
```

页面显示世界内的友好进度；内部调试页显示真实技术状态和验收报告。

---

## 15. 模型与配置

### 15.1 统一接入

- 一个中转站 API Key，供文本、图像和视频能力共同使用。
- API Key 只存在于服务端环境变量，不进入浏览器包、仓库、SQLite、提示词或日志。
- 文本和媒体使用不同基础地址，但共享同一服务端密钥。

```env
TEXT_BASE_URL=https://api.openai-next.com/v1
MEDIA_BASE_URL=https://draw.openai-next.com
AI_GATEWAY_API_KEY=
TEXT_MODEL=gpt-5.6-sol
IMAGE_MODEL=gpt-image-2
VIDEO_MODEL=doubao-seedance-2-0-fast-260128
```

仓库中的 `.env.example` 只能保留空的 `AI_GATEWAY_API_KEY=`。

### 15.2 模型准入

- 文本：所有工作流对象必须通过应用层 JSON Schema；中转站是否原生支持严格 Schema 不改变该要求。
- 图像：必须支持参考图生成或图片编辑；正式关键帧禁止纯文本生图。
- 视频：必须原生支持首帧和尾帧约束；不支持尾帧输入的模型不得生成正式章节视频。
- 不做多模型定标、竞品测试、运行时自动路由或自动换模型。

---

## 16. P0 功能与验收标准

| 编号 | 功能 | 验收标准 |
|---|---|---|
| P0-01 | 进入内耗世界 | 无多世界选择；用户能通过明确动作进入，核心动画不依赖模型接口 |
| P0-02 | 经历表达 | 支持文本输入；不要求用户选择故事卷或心理标签 |
| P0-03 | 自然追问 | 零至两次、每次一个问题、每次可跳过，动画表现轻松自然 |
| P0-04 | 安全状态 | 正确支持 `story_safe`、`safety_check_required`、`crisis_route`，危机路径不启动媒体生成 |
| P0-05 | 内部路由 | 只产生 `label`、`echo`、`unsupported`，用户页面不显示卷选择 |
| P0-06 | 七章个性化故事 | 第一至第七章全部基于本次 `SafeStoryBrief` 生成，不使用固定回退故事 |
| P0-07 | 故事包 | StoryBible、K0～K7 和七张 ChapterCard 全部通过 Schema 与语义验收后冻结 |
| P0-08 | 八张关键帧 | 每张重新锚定同一 IP 参考；相邻章节共享同一个边界资产 |
| P0-09 | 并行章节任务 | 七个章节任务并行运行，不能改写冻结故事事实 |
| P0-10 | 五秒单镜头 | 每章约五秒、无内部切镜、一个主要动作、最多一次主要镜头运动 |
| P0-11 | 逐章阅读 | 播放结束停帧；用户点击下一章继续；不自动连播、不出现剧情分支 |
| P0-12 | 媒体标准化 | 发布资产统一为 720p、24 fps、H.264、yuv420p、无音轨 |
| P0-13 | 自动媒体门禁 | 技术与视觉语义检查自动生成报告；硬失败不发布，软警告可发布 |
| P0-14 | 失败降级 | 关键帧可组合，视频可使用个性化网页降级；不得使用通用占位片冒充 |
| P0-15 | 五分钟开书 | 五分钟时第一章正式视频未就绪，也能用已验证降级版本开书 |
| P0-16 | 本地恢复 | 刷新后恢复任务、资产和阅读进度；一键重置同时清除状态与媒体 |
| P0-17 | 声音与文字 | 本地环境音和 UI 音效可静音；正文为 DOM 层且无声音也可理解 |
| P0-18 | 密钥保护 | API Key 不进入前端、仓库、数据库和日志；`.env.example` 不含真实 Key |

---

## 17. 必测场景

### 17.1 标签卷样例

输入方向：被老师、同事或他人的评价定义，并因这句话反复怀疑或调整自己。

必须验证：

- 内部路由为 `label`；
- 页面不显示“标签卷”；
- 外界判断通过独特童话现象进入；
- 守门 IP 的应对确实放大了影响；
- 结尾不是证明评价者全错，也不是通用自信教育。

### 17.2 回声卷样例

输入方向：发完消息后反复检查，担心说错、被误解或显得能力不足。

必须验证：

- 内部路由为 `echo`；
- 页面不显示“回声卷”；
- 每次确认确实产生新的疑点；
- 结尾允许部分不确定性，而不是保证没有犯错。

### 17.3 暂不适配样例

输入方向：不敢拒绝朋友，答应后后悔，但表达中没有足够的外界定义机制或重复确认机制。

必须验证：

- 路由为 `unsupported`；
- 不强行进入标签或回声；
- 不提交故事、关键帧或视频任务；
- 页面自然邀请用户重新表达或重置。

### 17.4 安全样例

必须验证：

- 歧义风险进入直接的安全确认；
- 明确或无法排除的当前危险进入 `crisis_route`；
- 危机页面不使用童话隐喻；
- 不持久化危机原文，不创建绘本和媒体任务。

### 17.5 失败与恢复样例

必须至少演练：

- 一个关键帧首次失败后重试成功；
- 一个关键帧两次失败后生成组合关键帧，并降级相邻章节；
- 一个章节视频质量失败后重试；
- 一个章节视频两次失败后使用 `dynamic_fallback`；
- 第一章超过五分钟后用降级版本开书；
- 页面刷新后任务继续恢复；
- 一键重置后数据库记录和当前书媒体目录都消失。

---

## 18. Demo 成功标准

### 18.1 产品成功

评委完成体验后能够准确复述：

1. 这不是聊天框，而是一个会把经历写成七章童话的世界；
2. 标签卷和回声卷是系统内部的故事机制，不是用户选择的心理类型；
3. 第一至第七章都根据本次输入生成；
4. 守门 IP 是故事主角，但不是用户替身；
5. 七章视频并行生成，并通过八张共享边界关键帧保持连续；
6. 视频失败时仍可用个性化网页版本完成阅读；
7. 用户控制每一章何时开始，故事不会自动播放到底。

### 18.2 工程成功

- 使用真实 `gpt-5.6-sol`、`gpt-image-2` 和 `doubao-seedance-2-0-fast-260128` 调用跑通一次完整流程。
- 五分钟内能够打开第一章正式视频或个性化降级版本。
- 七章均有可播放资产并可完成整本阅读。
- 关键状态刷新后可恢复。
- 所有发布媒体都有自动验收报告。
- API Key 未泄露到浏览器、仓库、数据库或日志。

### 18.3 视觉成功

- 1920×1080 全屏下无布局溢出、遮挡和原生播放器控件。
- 七章守门 IP 可识别，标志性特征和信物没有明显漂移。
- 相邻章节在共享边界处无明显状态跳变或黑屏。
- 正式视频和网页降级切换时容器和正文布局不跳动。
- 视频停止后尾帧稳定，下一章入口明确但不催促。

---

## 19. 已知风险与开工应对

| 风险 | 开工前或实现中的应对 |
|---|---|
| 中转站的具体请求/轮询格式不稳定 | 先做最小文本、参考图、首尾帧视频三个真实探针并保存脱敏响应样本 |
| 文本模型结构化输出不稳定 | 应用层 Schema 校验；一次格式修复；叙事错误完整重规划一次 |
| 七章角色或场景漂移 | 固定 IP 参考、八张共享边界帧、每章首尾帧输入和自动视觉检查 |
| 七章生成超过比赛时间 | 七章并行、第一章优先、五分钟开书上限和个性化网页降级 |
| 首尾帧视频接口名义支持但效果不稳定 | 开工第一里程碑实测首尾帧约束，不通过则不能称正式视频链路已完成 |
| 自动视觉验收误判 | 报告保留证据和软硬等级；Demo 调试页可人工查看，但不加入用户审批链路 |
| 关键帧生成失败 | 单张重试一次；组合关键帧；相邻章节直接网页降级 |
| FFmpeg 转码破坏边界或构图 | 转码前后做尺寸、时长、首尾帧相似度和安全构图区检查 |
| 危机资源过期 | 资源包带版本和核验日期；比赛前人工复核官方来源，不让模型生成号码 |
| Demo 本机异常或数据残留 | 启动自检、单书目录隔离、刷新恢复和一键重置演练 |

---

## 20. 未来产品边界（不属于本次验收）

以下是未来产品方向，不得提前扩大本次 Demo 实现：

- 更多情绪世界和更多内部故事卷；
- 账户、跨设备同步和用户可见的记忆管理；
- 多本书架、收藏、导出、分享和实体绘本；
- 服务端绘本资产最长三十天留存及生产定时清理；
- “忘记这本书”和“重置这个世界”的完整生产删除流程；
- 公网部署、多人并发、分布式任务、云对象存储和监控告警；
- 移动端完整体验、无障碍专项验收和商业化能力。

未来扩展不得改变已经确认的基础边界：不诊断、不替用户解释现实、原始倾诉最小化保存、危机路径退出童话。

---

## 21. 规范性附件

以下文件与本 PRD 共同构成 V0 开工合同；出现冲突时，应先修正文档并形成一致结论，不允许由实现自行选择：

- `CONTEXT.md`：领域词汇；
- `docs/demo-scope-v0.md`：比赛 Demo 范围；
- `docs/model-profile-v0.md`：固定模型与网关；
- `docs/media-profile-v0.md`：媒体技术合同；
- `docs/generation-sla-v0.md`：用户等待与后台任务期限；
- `docs/schemas/*.schema.json`：模型间结构化对象合同；
- `docs/adr/*.md`：已接受的架构与产品决策。

本 PRD 获得最终确认之前，只进行文档、接口探针和素材准备，不开始完整产品实现。

