# 藏梦书境：现场 Web Demo 手册

## 开场前 3 分钟

1. 确认电脑网络可用；世界入口和旅人页是本地资源，真实 AI 生图需要联网。
2. 确认项目的本地环境变量已经配置，真实密钥不得写入仓库或投屏展示。
3. 双击仓库根目录的 `start-demo.cmd`。
4. 等待终端显示 `demo preflight passed` 和本地地址。
5. 浏览器打开 `http://127.0.0.1:3000/` 后按 `F11` 全屏。
6. 推荐分辨率：`1920×1080` 或 `2048×1152`，浏览器缩放保持 `100%`。

## 3 分钟讲解路线

### 0:00–0:35 世界大门

- 说明这是“把心事写成童话”的世界入口。
- 拖动中央叶形门把，或按 `Enter` 开门。
- 强调原画全屏保留，交互只覆盖门把和门缝光，不破坏画面。

### 0:35–1:15 旅人初页

- 输入一个简短称呼，例如“小满”。
- 选择一枚口袋印记，例如“星星”。
- 展示按钮按下即反馈、印记可再次点击取消、右上角“减少动态”。
- 点击“沿着脚步继续”。

### 1:15–1:45 门厅

- 指出称呼和印记已经传递到后续界面。
- 点击唯一开放的“内耗之门”。

### 1:45–3:00 表达与真实生图

- 使用固定演示句，避免现场临时输入导致结果偏离：
  `我总担心自己做得不够好，明明很累，却还是不敢停下来。`
- 完成一轮可跳过追问，生成不含心事原文的安全故事摘要。
- 用摘要调用 `gpt-image-2`，展示明确的生成进度和最终童话插画。
- 如果接口失败，直接说明“真实生成未完成”并展示错误提示；不得把本地样例伪装成 AI 结果。

## 快速恢复

- 返回入口：点击左上角“藏梦书境”。
- 完整重置：使用页面内“重新体验入口”。
- 页面异常：按 `Ctrl+R` 刷新；本地称呼可能保留，但不保存心事原文。
- 服务异常：关闭终端，重新双击 `start-demo.cmd`。
- 端口占用：先关闭旧的 Demo 终端，再重新启动。
- AI 失败：先检查网络和环境变量；不要在投屏中打开或复制密钥文件。

## 现场范围

本轮只展示 Web 端：

- 世界大门；
- 旅人初页；
- 门厅与表达流程；
- `gpt-image-2` 真实生成和结果页。

暂不开发 App/原生端、登录、云同步、完整数据库、多世界、支付、分享和生产部署。
