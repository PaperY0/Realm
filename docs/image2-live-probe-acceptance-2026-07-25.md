# gpt-image-2 真实探针验收记录（2026-07-25）

## 结论

本记录只描述 2026 年 7 月 25 日已经发生的唯一一次真实图片生成探针。该次生成**未成功**，没有可验收图片；项目没有重试，也没有使用 fallback 或预制图片冒充结果。

## 已执行的付费探针

- 命令：`npm run image2:live`
- 明确付费确认：是
- 实际请求次数：1
- 固定模型：`gpt-image-2`
- 固定输出规格：`720x1280`、`medium`、PNG
- 参考图：世界大门图同时作为守门 IP 与风格参考
- 实际耗时：6406 ms
- HTTP 类别：`4xx`
- 安全错误码：`IMAGE_PROVIDER_ERROR`
- 输出图片：无
- 自动重试：0
- fallback：0

以上数据来自该次命令的脱敏终端输出。随后执行零费用配置检查时，旧版探针曾覆盖 `runtime/probe/latest.json`，因此不能把当前 `latest.json` 当作该次付费失败的原始文件证据。

## 已完成的无费用修复

1. 媒体基础地址已从旧示例统一为 `https://api.openai-next.com/v1`。
2. 零费用 `npm run image2:check` 已通过，确认配置文件、网关地址格式和参考图可读取；请求次数为 0。
3. 探针现会分别保存：
   - `runtime/probe/latest.json`：最近一次付费探针尝试；
   - `runtime/probe/latest-check.json`：最近一次零费用配置检查；
   - `runtime/probe/history/`：每次探针的独立历史记录。
4. 页面已移除真实生成的重试入口；同一条 SafeStoryBrief 最多发送一次请求。

## 尚未完成的验收

更新后的媒体地址还没有获得新的付费授权，因此没有再次发送真实请求。当前可以确认本地 Web 流程、安全摘要、失败状态和无占位图行为，但不能声称 `gpt-image-2` 已经成功生成现场图片。

如需继续真实链路验证，必须再次获得一次新的明确付费授权；每次授权只允许一个请求，失败不重试、不 fallback。
