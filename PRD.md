# 《藏梦书境》产品需求文档（PRD）

> 文档版本：V2.2（图片绘本优先版）
> 更新日期：2026-07-25
> 当前状态：核心体验与技术边界确认稿
> 产品阶段：Windows 本机、单实例、真实模型调用 Demo
> 唯一开放世界：内耗世界

---

## 1. 产品定义

### 1.1 一句话定义

《藏梦书境》是一个把用户本次经历转化为七章个性化图片童话绘本的互动 Web Demo。用户在内耗世界中自然表达一件难以消化的事，系统以固定的内耗守门 IP 为主角，根据本次输入生成完整故事和七张章节主插画；用户像阅读一本真实绘本一样逐章翻页。图片绘本是唯一默认生成结果，视频不进入 V0 生成链路。

### 1.2 品牌表达

- 正式名称：**藏梦书境**。
- 品牌副标题：**一个把心事写成童话的世界**。
- 入口文案：**推开门，让心事成为童话。**
- 合书文案：**你合上的每一本书，世界都会记得。**
- “藏”表示收藏与安放，不表示压抑情绪。
- 产品不得与“AI 心理治疗”“心理诊断”或治疗效果承诺并列宣传。

### 1.3 产品定位

产品提供情绪表达、童话转译和自我观看的体验，不提供心理疾病诊断、量表评估、治疗方案或现实关系裁决，也不替代心理咨询、医疗服务和现实社会支持。

核心价值是：

1. 用轻松、低压力的方式让用户说出一段经历；
2. 把现实张力转译为有距离感的童话因果；
3. 用连续、可阅读的七章图片绘本呈现本次独有故事；
4. 最大限度保留参考图的绘本质感，而不是追求廉价 3D 表演；
5. 让用户在故事中自行看见关系变化，而不是接收说教或通用安慰。

---

## 2. V0 目标与范围

### 2.1 本次必须证明的事情

```text
进入内耗世界
→ 自然表达与可跳过追问
→ 安全处理与内部故事路由
→ 根据本次输入生成完整七章故事
→ 冻结 StoryBible、StorybookStyleGuide、ChapterIllustrationPlan 与七张 ChapterCard
→ 提取安全回声词并选择固定视觉变体
→ image_selector 并行生成七张章节主插画，第一章优先
→ 第一章主插画通过门禁后尽快开书，其余章节继续并行准备
→ 用户通过真实翻书效果逐章阅读
→ 合书、入架并明确展示信物与世界变化
→ 刷新后由物件、地点和书脊印记证明“世界记得你”
```

**核心媒体边界：**V0 真正生成的是图片绘本。视频生成、图生视频和逐章视频增强都不属于本次 P0，不得阻塞开书，也不得被写成默认体验。

### 2.2 Demo 运行边界

- 一台 Windows 演示电脑；Chrome 或 Edge 全屏运行。
- 本地浏览器加本地 Node.js 服务端。
- 一次只有一名用户、一个浏览器会话、一本当前绘本。
- 使用真实文本和图像模型调用；生成结果是本次输入对应的完整故事与七张章节主插画。
- 章节翻页、纸张阴影、页背、书脊和轻量热点反馈由网页前端实现，不需要视频生成。
- 不用预生成故事或插画冒充本次结果；固定透明叠加素材只用于已声明的低成本视觉变体。
- 页面刷新后能够恢复当前绘本的生成任务、人工审核状态、已完成插画和阅读进度。
- 提供一键重置，清除当前书、任务状态和对应本地媒体。

### 2.3 本次明确不做

- 不开放其他情绪世界，也不展示多世界选择页。
- 不让用户选择标签卷或回声卷；两者只作为内置故事路由。
- 不做第三套内耗故事卷；无法可靠匹配时进入“暂不适配”。
- 不做注册、登录、账户找回、跨设备同步或多人并发。
- 不做公网部署、云对象存储、CDN、Redis、消息队列或高可用。
- 不做移动端完整适配；手机和平板只要求页面不崩溃。
- 不做剧情分支、对错选项、自动连续播放或自由 3D 探索。
- 不做 3D 建模、角色绑定、口型同步、完整走路或转身表演。
- 不做视频生成、图生视频、逐章视频增强、AI 配音、角色对白、个性化音乐或供应商原声。
- 不做付费、订阅、额度、导出、商业化、运营后台或生产级监控。

---

## 3. 目标用户与设计原则

### 3.1 核心用户

- 能够正常学习、工作和生活，但会因外界评价、害怕犯错、反复确认或反刍而持续内耗的成年人。
- 喜欢童话、绘本、治愈系 IP、轻叙事互动或具有收藏感的数字内容。
- 不想被直接分析、说教或贴心理标签，希望先用更含蓄的方式整理感受。

### 3.2 设计原则

1. **先交付核心卖点。** 从推门到看见第一章，目标不晚于第 90 秒；入口仪式服务于故事，不得反客为主。
2. **用户决定节奏，系统负责故事。** 用户可以表达、跳过追问、停留、翻页和退出，但不能改变剧情走向。
3. **第一至第七章都个性化。** 个性化发生在完整故事、章节文字、主插画、书封、信物、回声词和世界变化上。
4. **图片绘本是默认生成物。** 七张主插画与真实翻书交互构成完整体验；视频不是依赖，也不纳入 V0 开发。
5. **隐喻优先于解释。** 不直接复述现实经历，不替用户下结论，不把童话写成建议清单。
6. **因果约束，不固定剧情。** 模板约束故事机制和安全边界，不预先规定七章分别发生什么。
7. **一个规划者拥有故事事实。** 章节执行器只能渲染，不能改写故事。
8. **低成本个性化必须可见。** 优先使用安全回声词、书封字段和固定视觉变体，而不是依赖慢速媒体生成。
9. **世界记忆必须被看见。** 记忆通过书、信物、地点和环境变化表达，不以数据库口吻复述用户资料。
10. **失败保留可用结果。** 单章插画失败不回滚已完成章节，不用通用占位图伪装成功。
11. **安全状态描述工作流，不描述用户。** 不使用临床风险等级或人格判断。
12. **Demo 优先于生产设计。** 只实现比赛现场需要真实验证的最小闭环。

---

## 4. 世界、故事卷与路由

### 4.1 产品层级

```text
藏梦书境
└── 内耗世界（V0 唯一开放）
    ├── 标签卷（内部路由，不由用户选择）
    ├── 回声卷（内部路由，不由用户选择）
    └── 比较卷 / 岔路卷 / 边界卷 / 余光卷（只作不可点击预告）
```

- 页面不出现“进入标签卷”“进入回声卷”或任何选卷控件；用户只选择情绪世界，系统负责匹配故事卷。
- 用户看到的是自然的世界回应、故事标题和章节内容，不看到内部分类。
- 标签卷与回声卷不是心理类型，也不是对用户身份的判断。
- 其余四卷只以远处封闭书架格、未完成书脊或“正在编织”的线作为预告，不可点击，不让用户自我诊断。

### 4.2 路由结果

`StoryTemplateMatch` 只能得到以下一种结果：

- `label_template`
- `echo_template`
- `not_supported`

路由依据 SafeStoryBrief 与人工编写的模板匹配条件，记录命中条件、冲突条件、缺失信息和模板版本。路由结果不直接展示给用户。

### 4.3 标签卷机制

适用于外界评价、被定义、比较、认可依赖、自我价值被他人结论覆盖等经历。故事机制关注：

- 外界标签如何进入童话世界并改变主角对自身的观看；
- 主角如何发现标签只描述局部，而不拥有定义全部自我的权力；
- 结尾不是“证明别人错了”，而是重新取得命名与观看自己的权利。

### 4.4 回声卷机制

适用于害怕犯错、反复确认、完美主义、预演失败、事后反刍等经历。故事机制关注：

- 担忧如何在重复确认中变成越来越响的回声；
- 主角如何尝试更多确认，却发现确定感没有永久增加；
- 结尾不是保证不会出错，而是允许部分不确定性存在并继续行动。

---

## 5. 完整用户体验流程

### 5.1 第一次进入

#### 阶段 1：亲手打开童话世界大门（8～12 秒）

1. 页面载入后呈现关闭的童话世界大门和世界主 IP“拾页”，不以聊天框、自动播放视频或说明弹窗作为入口。
2. 用户长按门把并拖动；门缝、光线、纸屑与风声随进度变化。
3. 门打开后，拾页先走一步，再回头做出伸手动作；镜头随即自动进入短路径，不再要求用户额外点击角色的手。
4. 用户不阅读说明也应在 8 秒内理解门把可拖动；提供键盘替代与减少动态模式。

#### 阶段 2：旅人初页（15～20 秒）

1. 称呼树和口袋石滩位于同一连续场景，不拆成两个停顿节点。
2. 用户可选择或填写称呼；未选择时统一称为“旅人”。
3. 用户可选择星星、雨滴、蓝线、纸船或叶子作为个人印记；印记用于书签、书脊、当前书位和界面装饰，不参与路由。
4. 不填写称呼、不选择印记也可直接前进；空白叶会被拾页收进小包，不弹出强制提示。
5. 第一次记忆伏笔出现：

> “名字可以晚一点再想。路会先记得你的脚步。”

#### 阶段 3：门厅与进入内耗世界（15～20 秒）

1. 门厅只做一次明确点击：用户点击唯一开放的“内耗之门”。
2. 标签卷与回声卷不作为门展示；比较卷、岔路卷、边界卷和余光卷只在远处以不可点击的未完成书脊预告。
3. 内耗守门 IP“理线”在门边藏线、递出线头；拾页留在门厅，以目光完成交接。
4. 点击后镜头连续穿过门缝进入内耗世界，不增加二次确认。

#### 阶段 4：内耗路径到输入（10～15 秒）

1. 使用一次连续镜头移动完成路径，不做多个可停留景点，不产生剧情分支。
2. 镜头抵达一排尚未响起的风铃；会话需求纸条与输入框在同一风铃场景中出现。
3. 理线用克制语言邀请表达：

> “这里的风铃只替人保管说不出口的事。今天，有哪一句话还挂在你身上？”

#### 阶段 5：用户表达与轻量追问（20～40 秒）

1. 用户可任选“陪我待一会 / 帮我理一理 / 给我一点勇气 / 先留下来”，也可跳过；该选择只影响题记和合书回应语气。
2. 支持自由文字和“我还不知道怎么说”，不要求填写结构化问卷或给情绪打分。
3. 初始表达后最多追问两次，每次只问一个生成故事必需的信息；任意一轮可跳过。
4. 页面不展示心理标签、诊断结论或“模型正在分析你”。
5. 原始输入先进入安全检测，再进入内部故事路由；明确或无法排除的当前危险进入 `crisis_route`，停止故事与插画任务，不持久化原文。

示例：

> “那句话落下来时，你更像是想躲起来，还是想证明什么？”

#### 阶段 6：纸船交接（2～5 秒）

1. 用户确认表达完成后，理线把话折成纸船放入故事湖；仪式本身不超过 5 秒。
2. 后台完成安全摘要、内部卷匹配、七章规划、回声词提取和视觉变体选择。
3. `image_selector` 并行生成七张主插画，同时给予第一章最高优先级。
4. 前台使用书封、纸页、墨迹、标题和章节逐步成形表现进度，不使用纯转圈空白页。
5. 最晚在首次进入后的第 90 秒抵达可阅读的第一章；此目标是 P0 性能预算，不允许用预生成通用插画冒充。
6. 第一章满足故事包冻结、图片技术门禁和审核条件后即可开书；后续章节继续并行准备，但用户翻到某章前该章必须已通过门禁。

#### 阶段 7：七章互动图片绘本

1. 默认一册七章，每章以本次生成的一张章节主插画为视觉主体，配合网页 DOM 标题、正文和最多一个不改变剧情的热点反馈。
2. 每次从一章进入下一章，都必须出现可感知的真实翻书效果：书页从右侧抬起、弯曲并越过书脊，显示页背、纸张阴影与落页回弹，而不是简单横移、淡入淡出或轮播切图。
3. 标准翻页建议 450～700 ms；用户可点击页角、拖拽页边或使用“下一章”触发，同一次动作只翻一页。
4. 减少动态模式仍保留 150～250 ms 的页缘移动、书脊阴影变化和章节切换反馈，避免完全无反馈。
5. 小互动可以让纸签、灯、风、线头或信物产生轻量视觉回应，但不得改变故事路由、章节顺序、剧情因果或后续插画。
6. 用户可停留、返回上一章或退出；系统绝不自动连续翻到结尾。
7. 视频只是未来可选媒体槽位，不是本版本生成项；图片加载成功即可完整阅读。

#### 阶段 8：合书、入架、信物与结果

1. 第七章结束后，用户拖动封面或点击“轻轻合上”发起合书。
2. 书封和书脊明显属于用户：动态标题、称呼或“旅人”、日期、个人印记、卷的书脊图案、信物和用户主动留下的一句话至少展示其中六项。
3. 合书后不只播放“书飞入书架”，还要以童话化但清楚的方式展示：

```text
这一次，世界留下了：
一本书：《……》
一件信物：……
一处变化：……
```

4. 第二次记忆伏笔出现：

> “这本书不会消失。它会在这里保留一个位置。”

5. 理线只依据本次会话需求、已完成的内部故事卷、信物，以及用户明确允许保存的最小记忆卡给出 1～3 轮短回应；不保存完整倾诉原文。
6. 系统只有在入架动画完成且永久世界记忆保存成功后，才进入合书后的回声阶段。

### 5.2 再次进入

1. 第三次记忆验证必须由环境主动表现，而不是只读取并显示 localStorage 文本。
2. 至少恢复一种可见变化：天空里已有上次的风筝、第七码头的灯仍亮着、拾页或理线看一眼旧信物、书脊保留用户印记。
3. IP 不说“根据你上一次提供的信息”；它通过物件和地点表达记忆，例如：

> “那只风筝还在书架上方。今天的风没有把它吹走。”

4. 用户可以重读当前绘本、查看信物，或只在世界中停留而不表达。
5. V0 只保存当前这一册，不支持在保留当前书时生成下一册；开始新经历前必须明确重置。

### 5.3 首章价值交付时间预算

| 阶段 | 建议时长 |
|---|---:|
| 推门 | 8～12 秒 |
| 旅人初页 | 15～20 秒 |
| 门厅和进入内耗世界 | 15～20 秒 |
| 内耗路径到输入 | 10～15 秒 |
| 用户表达 | 20～40 秒 |
| 纸船交接 | 2～5 秒 |
| 抵达第一章 | 最晚第 90 秒 |

- 单阶段区间是设计预算，不应机械相加为允许总时长；前端过渡应重叠、自动衔接并尽量在区间下沿完成。
- 除推门、内耗之门点击、表达提交和翻页外，不增加阻断式点击。
- 跳过称呼、印记、会话需求或追问时，流程必须更快而不是出现替代等待。

## 6. 个性化故事生成

### 6.1 生成总流程

```text
用户输入
→ 安全检测
→ SafeStoryBrief
→ StoryTemplateMatch（标签 / 回声 / 暂不适配）
→ 全局故事规划器一次性生成 StoryBible、StorybookStyleGuide、ChapterIllustrationPlan 与七张 ChapterCard
→ 提取一个安全、抽象的回声词
→ 从固定素材库选择天气、纸签、环境光、书签与结尾天空变体
→ Schema 与独立叙事语义验收
→ 冻结故事包
→ image_selector 并行生成 I1～I7，I1 优先
→ 逐章图片门禁与人工审核
→ 网页绘本阅读器用真实翻书效果呈现
→ 合书保存信物、书籍字段与世界变化
```

视频生成不属于该流程。任何视频能力都不得成为第一章、后续章节、合书或刷新恢复的前置条件。

### 6.2 SafeStoryBrief

SafeStoryBrief 是去除不必要原始表述后的安全故事摘要，至少包含：

- 本次事件的非敏感事实；
- 涉及的关系或情境；
- 用户明确表达的感受与期待；
- 可用于童话转译的张力；
- 不得推断或不得进入故事的内容；
- 安全工作流结果；
- 本次会话需求；用户跳过时为 `null`，只作为题记与合书回应的语气参数。

它不能把模型推断写成用户事实，也不能保存危机路径原文。本次会话需求不得参与故事卷匹配、七章剧情生成或永久人格画像。

### 6.3 故事模板

每套 StoryTemplate 规定：

- 适用与排除条件；
- 核心童话因果机制；
- 可选场景、象征和宽松叙事锚点；
- 必须避免的说教、现实裁决和诊断表达；
- 结尾关系的变化方向。

模板不规定七章固定事件槽位，不提供预生成故事或媒体骨架。

### 6.4 全局故事规划器

全局规划器是唯一能创建和重规划故事事实的组件，负责一次性生成：

- 绘本标题；
- 七章完整因果；
- 守门 IP 的起始状态、变化与结尾状态；
- 反复象征、信物和世界规则；
- 每章叙事功能、可见事件和状态变化；
- StoryBible；
- StorybookStyleGuide；
- ChapterIllustrationPlan；
- 七张 ChapterCard。

### 6.5 冻结故事包

冻结故事包至少包括：

- SafeStoryBrief；
- StoryTemplateMatch 与所绑定 StoryTemplate 版本；
- StoryBible；
- StorybookStyleGuide；
- ChapterIllustrationPlan；
- 七张 ChapterCard；
- StoryPackageValidationReport。

所有对象通过 Schema 和叙事语义验收后转为只读。媒体执行器不得在生成失败时修改故事事实。

### 6.6 七个章节执行器

- 七张 ChapterCard 可分发给不同章节执行器并行准备图像提示和媒体任务。
- `image_selector` 根据统一网关能力选择图像生成路径并行生成七张章节主插画。
- 章节执行器只能把冻结合同翻译为供应商提示，不得新增人物、替换象征、改变章节功能或结尾状态。
- 插画审核通过后，网页阅读器读取该章插画、DOM 文字、热点配置和翻页合同进行展示。
- 章节执行器不生成视频，不把图片失败转交给视频模型修复。

### 6.7 可感知的低成本个性化

#### 书的个性化

每本书至少包含：动态标题、称呼或“旅人”、日期、个人印记、卷的书脊图案、本次信物和用户主动留下的一句话。示例：

> 《把没有署名的风筝放回天空》
> 旅人 · 2026.07.24
> 个人印记：蓝线

#### 章节回声词

- 从用户表达中提取一个安全、抽象、非身份化的回声词，例如“还不够”“再确认一次”“别出错”。
- 不直接复述现实人物、学校、公司、疾病、创伤细节或用户原句。
- 回声词只能进入童话物件、环境或角色动作，例如纸签轻轻写出“还不够”。
- `memoryAllowed=false` 时，回声词只存在于本次冻结故事包，不进入永久记忆。

#### 固定视觉变体

模型或规则只从审核后的固定素材中选择：

- 天气：薄雾、微雨、静风；
- 纸签：长签、方签、折角签；
- 环境光：月光蓝、晨雾灰、暖黄；
- 书签图案与章节热点反馈；
- 结尾天空变化。

这些素材是透明叠加或前端效果，不冒充实时生成插画；选择结果必须写入本书视觉合同，确保刷新后一致。

#### 明确结果

合书后必须让用户看懂本次经历留下了哪本书、哪件信物和哪处世界变化；不得只做抽象动画或数据结算页。

---

## 7. 长期 IP 角色圣经与视觉连续性

### 7.1 冻结身份

| 项目 | 世界主 IP | 内耗守门 IP |
|---|---|---|
| 名字 | **拾页** | **理线** |
| 世界职责 | 看见用户、带路、收起空白叶、在门厅交接 | 藏线、理线、递出线头、陪用户抵达表达处并担任七章主角 |
| 人格节奏 | 轻、好奇、先行动再邀请 | 慢、克制、允许停顿，不急着把结解开 |
| 语言示例 | “门好像认出你了。” | “不用一次说清。线也不是一下子打结的。” |

### 7.2 可识别轮廓

- **拾页：**较高而轻的身体比例，向后弯的单侧叶耳，短斗篷，小斜包，走路时包内露出一角空白叶；伸手和回头形成固定剪影。
- **理线：**较低而稳的身体比例，短圆耳或无耳帽轮廓，腰间线团，长尾或长围巾形成线状 S 曲线；递线头与放下线结形成固定剪影。
- 即使全黑剪影、缩略书签或远景中，两者也必须能仅靠轮廓和道具区分。

### 7.3 核心动作

**拾页：**看见用户 → 先走一步 → 回头伸手 → 把空白叶收进小包 → 在门厅完成交接。

**理线：**藏线 → 理线 → 递出线头 → 走几步回头 → 把打结的线暂时放下。

动作应成为加载、跳过、等待、交接和记忆反馈的统一语言，不为每个页面临时发明新表演。

### 7.4 语言差异

- 拾页句子较短、带一点发现感，更多描述门、路、叶子和光。
- 理线语速更慢、留白更多，更多描述线、结、风铃和可以暂时放下的东西。
- 两者不得共享同一套“温柔 AI”万能句式；同一页面不得连续轮流安慰用户。

### 7.5 禁止行为

两者都不得：

- 说“我完全理解你”或“我会永远陪着你”；
- 抢着解释用户、连续追问或替用户定义感受；
- 主动提起用户过去的敏感经历；
- 做心理诊断、现实关系裁决或治疗承诺；
- 把用户离开表现成遗弃角色，或用失落、等待惩罚用户；
- 使用“根据记录”“根据上次输入”等数据库口吻。

### 7.6 记忆方式

- IP 通过物件和地点表达记忆：旧信物仍在、书脊印记仍在、某盏灯仍亮、天空多了上次留下的图案。
- 用户明确允许保存的最小记忆卡只用于选择这些世界反馈，不直接复述原始倾诉。
- `memoryAllowed=false` 时不得保存 `safeSummary`、回声词或敏感推断；仍可保存非敏感的书籍完成事实、信物和世界变化。

### 7.7 插画连续性

- 每章一张主插画，编号 `I1` 至 `I7`。
- 每个插画任务携带同一套角色参考、固定轮廓、服装、材质、信物、绘本笔触和禁止变体。
- 跨章连续性由 StoryBible、StorybookStyleGuide、固定 IP、服装、信物、象征、章节状态与网页翻页共同维持。
- 七张插画必须人工检查角色识别、绘本质感、章节表达、构图和跨章连续性。

---

## 8. 章节媒体与技术规格

### 8.1 默认图片绘本合同

每章默认媒体是审核后的本章主插画：

- 每章一张，七章共七张；
- 竖屏 9:16，720×1280；
- PNG 或高质量 WebP；
- 章节文字由网页 DOM 层渲染，不烧录进图片；
- 图片加载失败时保留章节标题、正文、重试状态和明确错误，不用通用占位图冒充；
- 图片加载成功后无需等待任何视频或动态媒体即可阅读。

### 8.2 翻书交互合同

- 每章之间必须模拟真实纸页跨越书脊的翻动过程，包含页缘弯曲、页背、动态阴影和落页回弹。
- 标准时长 450～700 ms；拖拽时翻页进度与指针连续对应，松手后根据阈值完成或回弹。
- 翻页过程中不得黑屏、闪现下一章未加载图片或连续跳过两章。
- 翻页结束后焦点进入新章节标题；键盘左右方向键和可访问按钮必须可用。
- 减少动态模式使用 150～250 ms 的简化页缘与书脊反馈，不使用大幅 3D 翻转。

### 8.3 轻量视觉反馈

- 允许纸张呼吸、微弱光影、少量粒子和章节热点反馈，但这些是前端效果，不是生成视频。
- 轻量效果不得改变人物姿态、插画事实、章节顺序或用户对正文的阅读。
- 所有信息在关闭声音、减少动态和热点未触发时仍可理解。

### 8.4 视频边界

- V0 不接入视频模型、不提交图生视频任务、不为七章生成视频。
- 数据结构可以保留未来的可选媒体槽位，但其值默认为空，UI 不展示等待或升级提示。
- 未来若增加视频，必须是用户可忽略的逐章增强，失败不替换图片、不阻塞翻页，并通过独立 ADR 重新确认。

### 8.5 桌面视觉目标

- 正式视觉验收环境为 Windows Chrome / Edge 全屏，基准视口 1920×1080。
- 9:16 章节插画与书页、正文、页角和书脊构成完整桌面绘本界面，而不是简单居中手机图片。
- 1366×768 下保持布局、文字和核心操作完整。

---

## 9. 故事包验收

### 9.1 结构验收

至少检查：

- 所有 JSON 对象通过对应 Draft 2020-12 Schema；
- 版本、bookId、chapterId 和引用关系一致；
- 恰好七张 ChapterCard 与七项插画计划；
- 章节编号与 `I1` 至 `I7` 一一对应；
- 画幅、尺寸、翻页合同和人工门禁常量正确；
- 没有未声明字段、断裂引用或运行时任务状态混入冻结计划。

### 9.2 独立叙事语义验收

验收器只读检查：

- 七章是否都能追溯到本次 SafeStoryBrief，而不是通用故事；
- 所选模板机制是否真实进入因果；
- 七章状态变化是否连续，结尾是否由前文产生；
- 守门 IP 是否保持主角身份且不是用户替身；
- 插画瞬间是否与章节功能一致；
- 风格和连续性合同是否足够支持七个并行插画任务；
- 是否存在诊断、说教、现实裁决或不安全内容。

### 9.3 故事失败规则

- 格式或 Schema 错误只允许一次不改事实的格式修复。
- 叙事语义错误只允许全局规划器完整重规划一次。
- 第二次仍未通过则阻断本书全部插画任务，并邀请用户重新表达或重置。
- 验收器不能局部改写故事，也不能自行生成新的事实。

---

## 10. 图片门禁、人工审核与失败保留

### 10.1 章节插画门禁

每张插画先执行确定性检查，再由演示者人工审核。审核关注：

- 文件可解码、尺寸与安全性；
- 理线是否可识别且固定特征无明显漂移；
- 本章叙事瞬间、信物和象征是否正确；
- 绘本介质、笔触和纸张质感是否保留；
- 与前后章节的状态、服装、道具、色彩和光线是否连续；
- 固定视觉变体是否与本书合同一致。

### 10.2 渐进开书门禁

- 完整七章故事包必须先冻结，避免后续章节临时改写前文。
- 第一章插画通过技术门禁和人工审核后即可开书，不等待视频或七个动态预览。
- I2～I7 可继续并行生成和审核；用户翻到某章之前，该章必须已经通过门禁。
- 若下一章尚未就绪，当前页保持可读，并以世界内纸页状态提示等待；不得展示空白页或通用插画。

### 10.3 翻页门禁

至少检查：

- 点击、拖拽、键盘均只翻一页；
- 翻页时有页背、阴影、书脊和落页反馈；
- 标准与减少动态模式均无黑屏、闪烁、穿页和错误章节；
- 快速连续输入不会产生章节索引越界或不可能状态；
- 图片未就绪时不能完成翻入该章。

### 10.4 失败隔离

- 单章插画失败不回滚其他已批准插画，也不重新规划故事。
- 第一章失败时停留在可诊断等待状态；后续章失败时停留在前一可读页。
- 不允许媒体执行器为了迁就生成器而改写 ChapterCard 或 StoryBible。
- 不调用视频模型修复插画失败。

---

## 11. 时间预算、渐进生成与任务恢复

### 11.1 首章 90 秒目标

- 从首次推门开始计时，目标最晚第 90 秒进入可阅读第一章。
- 安全判断、故事规划、回声词提取、视觉变体选择和 I1 生成位于关键路径；I2～I7 与非关键装饰并行。
- 纸船动画只占 2～5 秒，不用长动画掩盖生成等待。
- 达不到 90 秒时显示可诊断的世界内等待状态，不用预生成通用图片冒充。

### 11.2 后续章节就绪

- I2～I7 在用户阅读第一章时继续并行生成与审核。
- 预计用户到达前仍未就绪的章节应提前提高队列优先级。
- 任一章节失败只重试该章；不得回滚已完成书页。

### 11.3 比赛演示时长

- 进入世界、表达并抵达第一章目标 60～90 秒。
- 七章图片阅读、合书和入架约 2～4 分钟，由用户翻页节奏决定。
- 视频不计入演示时长和端到端闭环。

### 11.4 刷新恢复

- 页面刷新后恢复故事包、插画任务、已审核图片、当前章节、合书状态和永久世界记忆。
- 原始输入、未完成追问、风险输入和模型内部理解不得通过刷新恢复。

---

## 12. 声音、文字与阅读体验

### 12.1 声音优先级

优先制作本地短音效：

- 门缝风声；
- 纸张摩擦与空白叶落下；
- 线轻轻拉紧；
- 风铃先不响、表达后才响起的反差；
- 纸船入水；
- 每章翻页；
- 合书与书飞入架；
- 回声河极轻环境声。

约束：

- 不调用音频模型，不突然自动播放大声音乐；首次用户手势后才可启用声音。
- 始终提供清晰静音开关，静音状态需持久化为非敏感偏好。
- 所有信息不用声音也能理解；减少动态模式仍保留必要的视觉反馈。
- 不使用过度煽情的钢琴音乐或声音提示用户“现在应该感动”。声音用于让世界更真实，不用于操纵情绪。

### 12.2 网页正文

- 每章标题与正文由 DOM 文字层渲染，不烧录进插画。
- 正文在静音、热点未触发和图片重试时仍可阅读。
- 回声词应融入物件或环境描述，不以高亮心理标签出现。

### 12.3 阅读控制

- 提供上一章、下一章、拖拽页角、静音和退出等绘本化控件。
- 不显示浏览器原生媒体控件，不自动推进章节。
- 每次章节切换都必须经过翻书效果或减少动态替代效果。

---

## 13. 隐私、身份与安全数据

### 13.1 匿名身份

- V0 不注册、不登录，以本机浏览器匿名标识恢复当前书。
- 不承诺跨设备、跨浏览器或清理浏览器数据后的恢复。

### 13.2 原始输入

- 原始表达只为形成 SafeStoryBrief 和完成必要安全判断短暂存在。
- SafeStoryBrief 成功生成后删除不再需要的原始输入。
- 数据库、日志、提示缓存和验收报告不保存完整原文。
- 模型推断必须与用户明确表述分开记录。

### 13.3 危机路径数据

- 危机路径不创建 bookId、故事包、插画任务或媒体任务。
- 不持久化危机原文。
- 联系信息只能来自赛前人工核验的资源包，不能由模型临时生成。
- 当前 V0 资源范围只面向中国大陆演示场景，正式扩展需重新核验地域规则。

### 13.4 删除边界

- 一键重置删除当前书的 SQLite 记录、相对路径引用和对应媒体目录。
- 不删除应用内置模板、守门 IP 和风格参考。
- 当前 Demo 不实现生产级三十天定时清理，但保留未来产品约束。

---

## 14. 本机技术架构

### 14.1 组件

```text
Browser UI
  ├── 世界进入、表达与自然追问
  ├── 图片绘本阅读器、翻书引擎与声音层
  ├── Zustand 会话状态
  ├── Zustand persist 永久世界记忆（partialize 白名单）
  └── 刷新恢复与重置
Local Node.js Service
  ├── Safety workflow
  ├── Text planner + schema validator
  ├── Story package validator
  ├── image_selector
  ├── Image validator + human review state
  ├── SQLite repository
  └── Local image storage
Unified relay
  ├── gpt-5.6-sol
  └── gpt-image-2
```

- API Key 只保存在本地服务端环境变量中。
- 前端不得获得 Key，不得直接调用中转站。
- 供应商响应必须脱敏后才能进入调试日志。

### 14.2 会话状态与永久世界记忆

**会话状态（内存态，不持久化）：**

- 当前步骤、原始输入、补全回答；
- 当前故事草案、内部理解、风险判断；
- 当前章节、图片加载状态、翻页进行状态；
- 本次会话需求和未提交的用户输入。

**永久世界记忆（白名单持久化）：**

- 称呼、个人印记、静音与减少动态偏好；
- 已完成书籍的最小字段；
- 信物、世界变化；
- 用户明确授权的最小记忆卡。

Zustand `persist` 必须使用 `partialize` 明确选择永久字段，禁止把整个 store 自动写入 localStorage。尤其禁止持久化原始倾诉、未完成草案、风险输入、模型内部理解和未授权的 `safeSummary`。

SQLite 保存生成任务和本书资产索引；图片文件保存到按 `bookId` 隔离的本地目录，不存为 BLOB。客户端永久记忆与服务端任务记录必须使用不同类型，不得互相整对象复制。

### 14.3 事件与状态转移

关键事件必须拆分：

```text
book + CLOSE_REQUESTED → closing
closing + BOOK_STORED → reflection
```

- `CLOSE_REQUESTED` 只表示用户发起合书。
- `BOOK_STORED` 只在入架动画完成、书籍记录保存、信物与世界变化提交成功后发出。
- 不再用同一个 `BOOK_CLOSED` 同时表示请求、动画完成和数据保存。

整书建议状态：

```text
entry → expression → planning → generating_illustrations
→ first_chapter_ready → reading → closing → reflection
```

插画建议状态：

```text
planned → queued → generating → validating → awaiting_human_review
→ approved | rejected | failed
```

### 14.4 不可能状态约束

类型与状态转移必须共同阻止：

- 没有冻结故事包却进入 `reading`；
- I1 未通过门禁却显示第一章；
- 当前章图片未批准却完成翻入该章；
- 第一章未读完却出现合书操作；
- 未收到 `BOOK_STORED` 就在书架显示新书或进入 `reflection`；
- 风险输入后仍保留上一份故事草案或插画任务；
- `memoryAllowed=false` 却持久化 `safeSummary`、回声词或敏感推断；
- 翻页中再次翻页导致章节索引越界；
- 重置后旧信物、世界变化或媒体路径仍可见。

---

## 15. 模型与配置

### 15.1 统一接入

- 文本中转基础地址：`https://api.openai-next.com/v1`
- 图像中转基础地址：`https://draw.openai-next.com`
- API Key：由本地环境变量统一提供，不进入仓库。
- 固定文本模型：`gpt-5.6-sol`
- 固定图像模型：`gpt-image-2`
- 模型名称、基础地址和 Key 只允许通过服务端配置读取。

`.env.example` 只包含变量名、已确认地址和模型名，Key 必须为空。

### 15.2 模型准入

开工第一阶段只对文本和图像链路做必要脱敏探针，确认：

- 文本模型可返回可解析的结构化故事包；
- 图像模型支持理线与风格参考输入，并能输出合格竖屏插画；
- 响应字段、错误格式和文件下载方式已记录。

V0 不配置视频模型，不做视频探针，不为未来可选能力延长当前实现。

---

## 16. P0 功能与验收标准

| P0 功能 | 验收标准 |
|---|---|
| 快速开门 | 用户亲手拖动开门；8 秒内理解；开门后自动进入短路径，不再点击拾页的手 |
| 合并旅人初页 | 称呼树与口袋石滩同场景；称呼、印记均可跳过且不阻断 |
| 唯一内耗之门 | 门厅只有一次明确点击；无选卷页面；其他卷仅不可点击预告 |
| 连续路径与输入 | 一次镜头抵达风铃；会话需求纸条和输入框同场景出现 |
| 表达与轻量追问 | 支持自由文字和不知道怎么说；最多两次单问题追问；任意跳过 |
| 首章价值交付 | 从推门到第一章目标不晚于 90 秒；纸船仪式 2～5 秒；不得用通用预生成图冒充 |
| 安全工作流 | 危机路径不请求故事或插画生成，不持久化原文，不显示付费或导出 |
| 内部路由 | 用户只选择内耗世界；系统只能匹配标签、回声或暂不适配，路由不展示 |
| 七章故事规划 | 第一至第七章都源自本次输入并通过 Schema 与语义验收 |
| 可感知个性化 | 动态书名、印记、日期、信物、回声词、固定视觉变体和世界变化可见 |
| 七张主插画 | `image_selector` 并行生成，I1 优先；每章一张 9:16、720×1280 图片 |
| 图片绘本阅读 | 图片即完整结果；每一章切换都有真实翻书效果；不自动翻页 |
| 双 IP 一致性 | 拾页与理线的名字、轮廓、动作、语言、禁止行为和记忆方式符合角色圣经 |
| 合书与结果 | `CLOSE_REQUESTED` 与 `BOOK_STORED` 分离；明确展示书、信物、世界变化 |
| 世界记忆 | 初页、合书、再次进入三次递进；重进后通过物件和地点表现变化 |
| 状态隐私 | Zustand persist 使用 `partialize`；原始倾诉、风险输入、草案与内部理解不持久化 |
| 刷新与重置 | 恢复当前书和永久世界变化；一键重置删除当前书记录与媒体 |
| 声音 | 有静音按钮；不用声音也能理解；不使用煽情音乐操纵情绪 |
| 密钥保护 | Key 不进入前端、仓库、数据库和日志 |

---

## 17. 必测端到端路径

### 17.1 路径一：标签卷完整闭环

```text
推门
→ 跳过称呼
→ 进入内耗世界
→ 输入评价相关文本
→ 内部匹配标签卷
→ 第一章最晚第 90 秒可读
→ 模拟或完成七章图片就绪并逐章翻页
→ 合书
→ 书架出现纸签风筝
→ 刷新
→ 书与世界变化仍存在
```

验证七章不是固定事件填空；结尾不是证明评价者错误，而是主角重新取得命名权。

### 17.2 路径二：回声卷资产隔离

验证：

- 命中回声卷；
- 信物是银鱼；
- 世界变化是第七码头灯；
- 不错误显示标签卷的纸签风筝、书脊或结尾天空素材；
- 故事不承诺“绝不会出错”。

### 17.3 路径三：风险出口

验证：

- 风险文本进入直接安全确认；
- 明确或无法排除的当前危险不请求故事或插画生成；
- 清除上一份未完成故事草案、回声词和插画任务引用；
- 不显示付费、导出或继续生成；
- 不持久化风险原文；
- 用户可以安全返回世界或离开。

### 17.4 路径四：逐章翻书与状态约束

验证：

- I1～I7 每次章节切换都有书页越过书脊的翻书效果；
- 点击、拖拽和键盘一次只翻一页；
- 快速连续操作不会越界或跳章；
- 下一章图片未批准时不能完成翻入；
- 减少动态模式仍保留必要翻页反馈；
- 第一章未完成时不出现合书；
- `CLOSE_REQUESTED` 后尚未 `BOOK_STORED` 时，书架不出现新书，回声阶段不启动。

### 17.5 失败、刷新与授权

至少演练：

- 一个插画任务首次失败后只重试该章并成功；
- 一张插画人工拒绝后只重做该章；
- 页面刷新后恢复当前书、已审核图片、阅读位置、信物和环境变化；
- `memoryAllowed=false` 时刷新后不存在 `safeSummary`、回声词和原始输入；
- 一键重置后数据库记录、当前书媒体目录、信物和世界变化都消失。

---

## 18. Demo 成功标准

### 18.1 产品成功

评委完成体验后能够准确复述：

1. 这不是聊天框，而是一个会把经历写成七章图片童话的世界；
2. 用户选择的是内耗世界，标签卷和回声卷由系统内部匹配；
3. 第一至第七章及其七张主插画都根据本次输入生成；
4. 拾页负责接待，理线负责引路并担任故事主角，两者不是用户替身；
5. 图片绘本与真实翻书效果构成默认完整体验，视频没有进入生成链路；
6. 用户能看见自己的书名、印记、回声词、信物和世界变化；
7. 世界通过物件和地点记住用户，而不是复述数据库记录。

### 18.2 工程成功

- 使用真实 `gpt-5.6-sol` 和 `gpt-image-2` 跑通一次完整默认流程。
- 第一章达到 90 秒价值交付目标，七张插画均可逐章阅读。
- 七次章节切换均通过真实翻书效果完成，无黑屏、跳章或索引越界。
- `CLOSE_REQUESTED`、`BOOK_STORED`、风险清理和 `memoryAllowed=false` 约束均由状态转移保证。
- 关键状态刷新后可恢复，敏感会话状态不被 persist，API Key 不泄露。

### 18.3 视觉成功

- 1920×1080 全屏下无布局溢出、遮挡和原生媒体控件。
- 七章理线可识别，标志性轮廓、服装和信物无明显漂移；拾页与理线不会混淆。
- 七张插画保持绘本介质、纸张纹理和统一光色语言。
- 每一章翻页都有页背、纸张阴影、书脊和落页反馈；减少动态模式仍清楚。
- 合书后用户能在 5 秒内看懂留下的书、信物和世界变化。

---

## 19. 已知风险与开工应对

| 风险 | 开工前或实现中的应对 |
|---|---|
| 入口仪式过长，用户未见核心卖点就离开 | 采用分阶段时间预算；除必要动作外自动衔接；首章最晚第 90 秒 |
| 七张图全部等待导致开书过慢 | 冻结完整故事后 I1 优先、I2～I7 并行，采用渐进开书门禁 |
| 用户觉得只是同一故事换几句话 | 动态书封、抽象回声词、固定视觉变体、信物与世界变化同时可见 |
| 回声词泄露或复述敏感原话 | 只允许安全、抽象、非身份化词；未授权不持久化 |
| 两个 IP 像同一种温柔 AI | 冻结名字、轮廓、动作、语言节奏和禁止行为；角色 QA 分开验收 |
| 世界记忆像 localStorage 功能展示 | 初页、合书、重进三次递进；用物件、地点、灯光和书脊表达 |
| 翻页只是轮播切图，缺少绘本感 | 实现页缘弯曲、页背、动态阴影、书脊和回弹；单独 E2E 验收 |
| Zustand 意外保存原始倾诉或风险内容 | 会话与永久 store 分型；`partialize` 白名单；授权与风险清理测试 |
| 合书事件语义混乱 | 拆分 `CLOSE_REQUESTED` 和 `BOOK_STORED`，保存成功后才进入回声 |
| 七张插画角色或风格漂移 | 固定 IP 参考、StorybookStyleGuide、ChapterIllustrationPlan 与逐章审核 |
| 中转站请求或响应格式不稳定 | 只做文本和图像两类必要脱敏探针；记录失败状态，不扩展视频链路 |
| 危机资源过期 | 资源包带版本和核验日期；比赛前人工复核官方来源 |
| Demo 本机异常或数据残留 | 启动自检、单书目录隔离、刷新恢复和一键重置演练 |

---

## 20. 未来产品边界（不属于本次验收）

- 更多情绪世界和更多内部故事卷；
- 账户、跨设备同步和用户可见的记忆管理；
- 多本书架、收藏、导出、分享和实体绘本；
- 服务端绘本资产最长三十天留存及生产定时清理；
- 公网部署、多人并发、分布式任务、云对象存储和监控告警；
- 移动端完整体验、无障碍专项验收和商业化能力；
- 可选的逐章图生视频或其他动态增强。

未来扩展不得改变已经确认的基础边界：不诊断、不替用户解释现实、原始倾诉最小化保存、危机路径退出童话。若未来增加视频链路，必须新增 ADR，并保证图片绘本始终可独立完成、视频失败不阻塞阅读。

---

## 21. 规范性附件

本轮产品决策以本 PRD V2.2 与 `CONTEXT.md` 为当前规范来源。

以下文件仍是实现支持材料，但其中关于“默认 2.5D 动效、Seedance、七章视频预览全部完成后才开书、五分钟开书”的旧描述已被 V2.2 明确取代，在同步更新前不得作为实现依据：

- `docs/demo-scope-v0.md`：比赛 Demo 范围；
- `docs/model-profile-v0.md`：固定模型与网关；
- `docs/media-profile-v0.md`：媒体合同；
- `docs/generation-sla-v0.md`：生成时限；
- `docs/adr/*.md`：历史架构与产品决策。

`docs/schemas/*.schema.json` 继续作为结构化对象合同；若 Schema 仍要求视频或动态字段，实施前必须将其改为可空或移除。任何实现冲突均以“图片绘本是默认完整结果、视频不进入 V0、首章目标最晚第 90 秒、每章使用真实翻书效果”为准。
