---
status: accepted
---

# 按模态固定上游模型而不使用网关自动路由

V0 虽然通过统一 API Key、统一中转站和统一接入层调用生成能力，但文本、图像和视频任务分别配置明确的上游模型。生成流程不使用中转站的自动选模、最优模型或随机路由能力。

模型名称通过配置管理，可以在不修改故事工作流的情况下整体替换。每个任务记录请求指定的模型、实际返回的模型或版本以及中转站任务编号；若中转站无法返回版本信息，则至少记录模型标识与请求时间。

该选择牺牲中转站自动优化价格和可用性的便利，以换取跨章节画风一致性、比赛现场的可复现性、成本可预测性和故障定位能力。
