---
status: accepted
---

# Demo 使用 SQLite 保存状态并将媒体放在本地目录

本机 Demo 使用单个 SQLite 文件保存绘本、插画任务、插画审核、2.5D 动效任务、可选增强任务和验收状态；章节主插画、2.5D 动态预览与可选增强媒体保存到按 bookId 隔离的本地目录，数据库中只记录相对路径。与共同改写一个 JSON 文件相比，SQLite 更适合七个并行插画与动效任务独立更新并支持刷新恢复，同时仍不需要运行外部数据库；一键重置同时清除当前书的数据库记录和媒体目录。
