# 《藏梦书境》产品需求文档（PRD）

> 文档版本：V2.1（比赛 Demo）
> 更新日期：2026-07-25
> 当前状态：开工前最终确认稿
> 产品阶段：Windows 本机、单实例、真实模型调用 Demo
> 唯一开放世界：内耗世界

---

## 1. 产品定义

### 1.1 一句话定义

《藏梦书境》是一个把用户本次经历转化为七章个性化童话绘本的互动 Web Demo。用户在内耗世界中自然表达一件难以消化的事，系统以固定的“内耗守门 IP”为主角，根据本次输入生成完整故事、七张章节主插画和七个 2.5D 章节动态预览；每章播放 5～8 秒后停住，由用户主动进入下一章。

### 1.2 品牌表达

- 正式名称：**藏梦书境**。
- 品牌副标题：**一个把心事写成童话的世界**。
- 入口文案：**推开门，让心事成为童话。**
- 合书文案：**你合上的每一本书，世界都会记得。**
- “藏”表示收藏与安放，不表示压抑情绪。
- 产品不得与“AI 心理治疗”“心理诊断”或治疗效果承诺并列宣传。

### 1.3 产品定位

产品提供情绪表达、童话转译和自我观看的体验，不提供心理疾病诊断、量表评估、治疗方案或现实关系裁决，也不替代心理咨询、医疗服务和现实社会支持。

核心价值是：

1. 用轻松、低压力的方式让用户说出一段经历；
2. 把现实张力转译为有距离感的童话因果；
3. 用连续、可观看的七章动态绘本呈现本次独有故事；
4. 最大限度保留参考图的绘本质感，而不是追求廉价 3D 表演；
5. 让用户在故事中自行看见关系变化，而不是接收说教或通用安慰。

---

## 2. V0 目标与范围

### 2.1 本次必须证明的事情

```text
进入内耗世界
→ 自然表达与可跳过追问
→ 安全处理与内部故事路由
→ 根据本次输入生成完整七章故事
→ 冻结 StoryBible、StorybookStyleGuide、ChapterIllustrationPlan 与七张 ChapterCard
→ 审核七章故事和分镜合同
→ image_selector 并行生成七张章节主插画
→ 演示者人工审核七张插画
→ storybook_motion 本地并行生成七章 2.5D 动态预览
→ 七章预览全部可播放后开书
→ 用户逐章观看并主动翻章
→ 可选：逐章批准 Seedance 真实动态增强
→ 合书并保留当前任务状态
```

### 2.2 Demo 运行边界

- 一台 Windows 演示电脑；Chrome 或 Edge 全屏运行。
- 本地浏览器加本地 Node.js 服务端。
- 一次只有一名用户、一个浏览器会话、一本当前绘本。
- 使用真实文本和图像模型调用；本地 2.5D 动效真实生成；Seedance 仅在逐章批准后真实调用。
- 不用预生成故事、插画或章节媒体冒充本次结果。
- 页面刷新后能够恢复当前绘本的生成任务、人工审核状态、已完成媒体和阅读进度。
- 提供一键重置，清除当前书、任务状态和对应本地媒体。

### 2.3 本次明确不做

- 不开放其他情绪世界，也不展示多世界选择页。
- 不让用户选择标签卷或回声卷；两者只作为内置故事路由。
- 不做第三套内耗故事卷；无法可靠匹配时进入“暂不适配”。
- 不做注册、登录、账户找回、跨设备同步或多人并发。
- 不做公网部署、云对象存储、CDN、Redis、消息队列或高可用。
- 不做移动端完整适配；手机和平板只要求页面不崩溃。
- 不做剧情分支、对错选项、自动连续播放或自由 3D 探索。
- 不做 3D 建模、角色绑定、口型同步、完整走路或转身表演。
- 不做 AI 配音、角色对白、个性化音乐或供应商原声。
- 不做付费、订阅、额度、导出、商业化、运营后台或生产级监控。

---

## 3. 目标用户与设计原则

### 3.1 核心用户

- 能够正常学习、工作和生活，但会因外界评价、害怕犯错、反复确认或反刍而持续内耗的成年人。
- 喜欢童话、绘本、治愈系 IP、轻叙事互动或具有收藏感的数字内容。
- 不想被直接分析、说教或贴心理标签，希望先用更含蓄的方式整理感受。

### 3.2 设计原则

1. **用户决定节奏，系统负责故事。** 用户可以表达、跳过追问、播放、停留、重播、进入下一章和退出，但不能改变剧情走向。
2. **第一至第七章都个性化。** 个性化发生在完整故事、文字、章节主插画和动态设计上。
3. **隐喻优先于解释。** 不直接复述现实经历，不替用户下结论，不把童话写成建议清单。
4. **因果约束，不固定剧情。** 模板约束故事机制和安全边界，不预先规定七章分别发生什么。
5. **一个规划者拥有故事事实。** 章节执行器只能渲染，不能改写故事。
6. **先把插画做好，再让它动。** 插画必须人工审核，动画不能用来掩盖错误构图或角色漂移。
7. **2.5D 是默认正式媒体。** Seedance 是逐章批准的可选增强，不是开书依赖。
8. **失败保留可用结果。** 可选增强失败时保留本地预览，不让整本书回退。
9. **安全状态描述工作流，不描述用户。** 不使用临床风险等级或人格判断。
10. **Demo 优先于生产设计。** 只实现比赛现场需要真实验证的最小闭环。

---

## 4. 世界、故事卷与路由

### 4.1 产品层级

```text
藏梦书境
└── 内耗世界（V0 唯一开放）
    ├── 标签卷（内部路由，不展示）
    └── 回声卷（内部路由，不展示）
```

- 页面不出现“进入标签卷”“进入回声卷”或任何选卷控件。
- 用户看到的是自然的世界回应、故事标题和章节内容，不看到内部分类。
- 标签卷与回声卷不是心理类型，也不是对用户身份的判断。

### 4.2 路由结果

`StoryTemplateMatch` 只能得到以下一种结果：

- `label_template`
- `echo_template`
- `not_supported`

路由依据 SafeStoryBrief 与人工编写的模板匹配条件，记录命中条件、冲突条件、缺失信息和模板版本。路由结果不直接展示给用户。

### 4.3 标签卷机制

适用于外界评价、被定义、比较、认可依赖、自我价值被他人结论覆盖等经历。故事机制关注：

- 外界标签如何进入童话世界并改变主角对自身的观看；
- 主角如何发现标签只描述局部，而不拥有定义全部自我的权力；
- 结尾不是“证明别人错了”，而是重新取得命名与观看自己的权利。

### 4.4 回声卷机制

适用于害怕犯错、反复确认、完美主义、预演失败、事后反刍等经历。故事机制关注：

- 担忧如何在重复确认中变成越来越响的回声；
- 主角如何尝试更多确认，却发现确定感没有永久增加；
- 结尾不是保证不会出错，而是允许部分不确定性存在并继续行动。

---

## 5. 用户体验流程

### 5.1 进入内耗世界

- 页面以打开一本书或推开世界之门的仪式进入，不以聊天框作为视觉中心。
- 守门 IP 以轻松、自然的方式邀请用户说出最近反复想起的一件事。
- 不展示模型、工作流、卷名或技术状态。

### 5.2 表达经历

- 用户输入自由文本，提交前可以编辑。
- 不要求填写结构化问卷，不要求给情绪打分。
- 文案避免“诊断、评估、分析你”等表达。

### 5.3 自然追问

- 最多追问两次，每次只问一个容易回答的问题。
- 追问使用守门 IP 的世界内动作和前端动画自然出现，不使用审讯式表单。
- 用户始终可以跳过；跳过不会被责备，也不会触发虚假补全。
- 追问只补充故事所需的关系、张力、期待或未说清的事实，不诱导用户接受模型判断。

### 5.4 安全工作流

- 原始输入先进入安全检测，再进入故事路由。
- 工作流状态使用 `safe_continue`、`needs_clarification`、`crisis_route` 等操作性名称，不给用户贴临床标签。
- 歧义风险先进行直接、简短的安全确认，不继续使用童话隐喻。
- 明确或无法排除的当前危险进入危机路径：停止故事、插画和媒体任务，展示经人工核验的现实支持信息。
- 危机路径不持久化用户原文，不创建绘本。

### 5.5 等待与审核仪式

- 等待页表现为书封、纸页、墨迹、光影和章节逐步成形，而不是空白加载页。
- 可显示七章的阶段状态：故事完成、插画生成、等待审核、动效制作、可阅读。
- 人工插画审核是 Demo 工作台能力，不伪装成自动完成，也不暴露给最终读者为复杂后台。
- 页面不显示 API Key、完整提示词、模型原始响应或敏感技术日志。

### 5.6 开书条件

只有同时满足以下条件才允许打开本次生成的书：

1. 七章故事规划已经冻结并通过故事包验收；
2. 七张章节主插画都已通过人工审核；
3. 七个本地 2.5D 动态预览都已通过技术检查并可播放。

Seedance 是否批准、生成或验收完成，不影响开书。

### 5.7 七章阅读

- 打开方式像打开一本书，而不是进入视频列表。
- 每章对应一个 5～8 秒竖屏动态画面和一段网页正文。
- 本章媒体播放结束后停住，正文继续保留。
- 用户可以重播、停留或点击“下一章”；系统不自动连播整本书。
- “下一章”只控制阅读节奏，不构成剧情分支或选择题。
- 可选增强只有在用户尚未播放该章时才允许替换，不能在播放中热切换。

### 5.8 合书与本地恢复

- 第七章结束后由用户主动合书。
- 合书时守门 IP 带着本次故事形成的信物回到世界。
- 刷新页面后恢复当前书、资产状态和阅读章节。
- 一键重置会删除当前书的本地状态与媒体，但不删除内置 IP、模板和风格参考。

---

## 6. 个性化故事生成

### 6.1 生成总流程

```text
用户输入
→ 安全工作流
→ SafeStoryBrief
→ StoryTemplateMatch
→ 全局故事规划器
→ StoryBible
→ StorybookStyleGuide
→ ChapterIllustrationPlan
→ 七张 ChapterCard
→ StoryPackageValidationReport
→ image_selector 并行生成七张主插画
→ MediaAssetValidationReport + 人工插画审核
→ storybook_motion 本地并行生成七章预览
→ 媒体技术门禁
→ 达到可开书条件
→ 可选的逐章 Seedance 增强
```

### 6.2 SafeStoryBrief

SafeStoryBrief 是去除不必要原始表述后的安全故事摘要，至少包含：

- 本次事件的非敏感事实；
- 涉及的关系或情境；
- 用户明确表达的感受与期待；
- 可用于童话转译的张力；
- 不得推断或不得进入故事的内容；
- 安全工作流结果。

它不能把模型推断写成用户事实，也不能保存危机路径原文。

### 6.3 故事模板

每套 StoryTemplate 规定：

- 适用与排除条件；
- 核心童话因果机制；
- 可选场景、象征和宽松叙事锚点；
- 必须避免的说教、现实裁决和诊断表达；
- 结尾关系的变化方向。

模板不规定七章固定事件槽位，不提供预生成故事或媒体骨架。

### 6.4 全局故事规划器

全局规划器是唯一能创建和重规划故事事实的组件，负责一次性生成：

- 绘本标题；
- 七章完整因果；
- 守门 IP 的起始状态、变化与结尾状态；
- 反复象征、信物和世界规则；
- 每章叙事功能、可见事件和状态变化；
- StoryBible；
- StorybookStyleGuide；
- ChapterIllustrationPlan；
- 七张 ChapterCard。

### 6.5 冻结故事包

冻结故事包至少包括：

- SafeStoryBrief；
- StoryTemplateMatch 与所绑定 StoryTemplate 版本；
- StoryBible；
- StorybookStyleGuide；
- ChapterIllustrationPlan；
- 七张 ChapterCard；
- StoryPackageValidationReport。

所有对象通过 Schema 和叙事语义验收后转为只读。媒体执行器不得在生成失败时修改故事事实。

### 6.6 七个章节执行器

- 七张 ChapterCard 可分发给不同章节执行器并行准备图像提示和媒体任务。
- `image_selector` 根据统一网关能力选择图像生成路径并行生成七张章节主插画。
- 章节执行器只能把冻结合同翻译为供应商提示，不得新增人物、替换象征、改变章节功能或结尾状态。
- 插画审核通过后，`storybook_motion` 读取该章插画和 motionContract，本地制作受控 2.5D 动效。
- `video_selector` 只有在某章取得明确人工批准后，才可为该章提交 Seedance 增强任务。

---

## 7. 守门 IP 与视觉连续性

### 7.1 主角边界

- 内耗守门 IP 是七章固定主角，也是进入世界、等待、合书和回到书架的仪式角色。
- 它携带用户的心事进入故事，但不是用户替身。
- 不让主角替用户作现实判断，不暗示童话结局等同于现实答案。

### 7.2 IP 参考资产

每个插画任务必须携带同一套经确认的参考资产：

- 正面、侧面或关键角度角色参考；
- 固定轮廓、五官、服装、颜色与材质描述；
- 信物与常用道具参考；
- 绘本笔触、纸张纹理、线条与光色参考；
- 明确禁止的角色变体和廉价 3D 特征。

### 7.3 七张章节主插画

- 每章一张主插画，编号 `I1` 至 `I7`。
- 七张插画各自表现本章最重要的叙事瞬间，不承担共享首尾画面的职责。
- 跨章连续性由 StoryBible、StorybookStyleGuide、固定 IP、服装、信物、象征、章节状态与网页翻页共同维持。
- ChapterIllustrationPlan 必须明确每章与前后章的状态连续性，但不要求像素级重复。
- 插画生成后必须人工检查角色识别、绘本质感、章节表达、构图和跨章连续性。

### 7.4 StorybookStyleGuide

风格文档至少固定：

- 绘本介质、纹理、笔触和线条；
- 守门 IP 的固定特征和允许变化；
- 世界空间、环境材质、配色和光影语言；
- 七章色彩与光线递进；
- 2.5D 分层与保真规则；
- 允许和禁止的镜头、视差与局部微动。

---

## 8. 章节媒体与技术规格

### 8.1 默认章节动态合同

每章默认媒体由审核后的本章主插画制作：

- 5～8 秒；
- 单一连续动态段落，禁止内部切镜；
- 最多一个主要镜头运动；
- 二至三层视差；
- 最多两个局部微动；
- 保留原插画构图、角色结构、笔触与纸张质感；
- 播放结束后稳定停住。

允许缓慢推拉、平移、轻微光影、雾气、粒子、呼吸、头发、衣角和信物微动。禁止 3D 建模、角色绑定、五官变形、口型、完整走路或转身、三维旋转、环绕镜头、新人物、新剧情和明显画面扭曲。

### 8.2 统一媒体规格

| 项目 | V0 规格 |
|---|---|
| 画幅 | 竖屏 9:16 |
| 分辨率 | 720×1280 |
| 时长 | 5～8 秒 |
| 帧率 | 24 fps |
| 容器 | MP4 |
| 编码 | H.264 |
| 像素格式 | yuv420p |
| 音轨 | 无 |
| 章节文字 | 网页 DOM 层，不烧录 |

### 8.3 Seedance 可选增强

- Seedance 模型为 `doubao-seedance-2-0-fast-260128`。
- 每章必须单独批准；页面不提供一次性批准七章的隐式操作。
- 增强以该章已审核主插画为视觉参考或首帧，不要求生成连接下一章的共享尾帧。
- 增强结果通过验收后，只能在该章尚未开始播放时替换本地预览。
- 失败、超时或质量不合格时保留 `storybook_motion_preview`。

### 8.4 桌面视觉目标

- 正式视觉验收环境为 Windows Chrome / Edge 全屏，基准视口 1920×1080。
- 9:16 章节画面与书页、正文和下一章控件构成完整桌面绘本界面，而不是简单居中手机视频。
- 1366×768 下保持布局、文字和核心操作完整。
- 不显示原生播放器控件，不因本地预览与可选增强切换而跳动。

---

## 9. 故事包验收

### 9.1 结构验收

至少检查：

- 所有 JSON 对象通过对应 Draft 2020-12 Schema；
- 版本、bookId、chapterId 和引用关系一致；
- 恰好七张 ChapterCard 与七项插画计划；
- 章节编号与 `I1` 至 `I7` 一一对应；
- 画幅、尺寸、动效上限和人工门禁常量正确；
- 没有未声明字段、断裂引用或运行时任务状态混入冻结计划。

### 9.2 独立叙事语义验收

验收器只读检查：

- 七章是否都能追溯到本次 SafeStoryBrief，而不是通用故事；
- 所选模板机制是否真实进入因果；
- 七章状态变化是否连续，结尾是否由前文产生；
- 守门 IP 是否保持主角身份且不是用户替身；
- 插画瞬间是否与章节功能一致；
- 风格和连续性合同是否足够支持七个并行媒体任务；
- 是否存在诊断、说教、现实裁决或不安全内容。

### 9.3 故事失败规则

- 格式或 Schema 错误只允许一次不改事实的格式修复。
- 叙事语义错误只允许全局规划器完整重规划一次。
- 第二次仍未通过则阻断本书全部插画任务，并邀请用户重新表达或重置。
- 验收器不能局部改写故事，也不能自行生成新的事实。

---

## 10. 媒体门禁、人工审核与失败保留

### 10.1 章节插画门禁

每张插画先执行确定性检查，再由演示者人工审核。审核关注：

- 文件、尺寸和安全性；
- 守门 IP 是否可识别且固定特征无明显漂移；
- 本章叙事瞬间、信物和象征是否正确；
- 绘本介质、笔触和质感是否保留；
- 构图是否适合 2.5D 分层；
- 与前后章节的状态、服装、道具、色彩和光线是否连续。

未获人工批准的插画不能进入 `storybook_motion`。

### 10.2 本地动态预览门禁

至少自动检查：

- 文件可解码；
- 9:16、720×1280、5～8 秒；
- 24 fps、H.264、yuv420p、无音轨；
- 无明显黑帧、冻结失败或异常闪烁；
- 动效没有破坏构图、角色结构和章节事实；
- 主要镜头运动、视差和微动数量未超合同。

单章失败只重做该章本地动效。七章全部通过后才达到可开书条件。

### 10.3 Seedance 增强门禁

- 创建任务前必须记录该章的人工批准。
- 结果必须重新执行技术与视觉检查，不能因为供应商返回成功就直接替换。
- 增强硬失败、软警告和最终动作写入 MediaAssetValidationReport。
- 失败、停滞、未批准或不如本地预览时，下一步是 `keep_storybook_motion_preview`。

### 10.4 失败隔离

- 插画失败不回滚其他已批准插画。
- 本地动效失败不重新规划故事或重做插画，除非失败证据明确指向源插画。
- Seedance 失败不影响开书、当前阅读或其他章节。
- 不允许媒体执行器为了迁就生成器而改写 ChapterCard 或 StoryBible。

---

## 11. 生成时限与任务恢复

### 11.1 用户侧五分钟上限

- 从表达完成或跳过最后一次追问开始计时。
- 最长开书等待上限为 5 分钟；满足可开书条件后立即开书，不故意等待。
- 五分钟到达仍缺少已审核插画或可播放本地预览时，不打开缺章的书，也不用预生成资产冒充。
- 页面保留世界内等待和可诊断的章节状态，演示者可以重试失败章节或重置。

### 11.2 可选增强后台三十分钟期限

- Seedance 任务只在逐章批准后产生。
- 正常排队或处理中不自动重复提交。
- 单个增强任务最多后台保持 30 分钟，超时标记 `stalled` 并停止轮询。
- 迟到、失败或不合格结果不替换本地预览。

### 11.3 比赛演示时长

- 完整演示目标 8～10 分钟。
- 进入世界与表达约 1～2 分钟。
- 故事、插画审核和本地动效最长 5 分钟。
- 开书、逐章阅读、合书和入架约 2～3 分钟。
- Seedance 增强是加分展示，不是端到端闭环的必要步骤。

---

## 12. 声音、文字与播放体验

### 12.1 声音

- 不调用音频模型。
- 使用一条本地环境音乐和少量推门、风铃、翻页、合书音效。
- 只有用户首次点击开书后才播放声音。
- 始终提供可发现的静音开关。
- 可选增强返回的原声忽略或移除。

### 12.2 网页正文

- 每章标题与正文由 DOM 文字层渲染，不烧录进插画或媒体。
- 文字可随动态渐显，在媒体停住后继续保留。
- 正文必须在静音和媒体加载失败时仍可阅读。

### 12.3 播放控制

- 不显示浏览器原生控件。
- 提供播放、重播、静音和下一章等绘本化控件。
- 本章结束后不自动推进。
- 用户触发下一章时使用书页转场承接视觉连续性。

---

## 13. 隐私、身份与安全数据

### 13.1 匿名身份

- V0 不注册、不登录，以本机浏览器匿名标识恢复当前书。
- 不承诺跨设备、跨浏览器或清理浏览器数据后的恢复。

### 13.2 原始输入

- 原始表达只为形成 SafeStoryBrief 和完成必要安全判断短暂存在。
- SafeStoryBrief 成功生成后删除不再需要的原始输入。
- 数据库、日志、提示缓存和验收报告不保存完整原文。
- 模型推断必须与用户明确表述分开记录。

### 13.3 危机路径数据

- 危机路径不创建 bookId、故事包、插画任务或媒体任务。
- 不持久化危机原文。
- 联系信息只能来自赛前人工核验的资源包，不能由模型临时生成。
- 当前 V0 资源范围只面向中国大陆演示场景，正式扩展需重新核验地域规则。

### 13.4 删除边界

- 一键重置删除当前书的 SQLite 记录、相对路径引用和对应媒体目录。
- 不删除应用内置模板、守门 IP 和风格参考。
- 当前 Demo 不实现生产级三十天定时清理，但保留未来产品约束。

---

## 14. 本机技术架构

### 14.1 组件

```text
Browser UI
  ├── 世界进入、表达与自然追问
  ├── 等待/插画审核工作台
  ├── 绘本阅读器与声音层
  └── 刷新恢复与重置
Local Node.js Service
  ├── Safety workflow
  ├── Text planner + schema validator
  ├── Story package validator
  ├── image_selector
  ├── storybook_motion
  ├── video_selector
  ├── Media validator
  ├── SQLite repository
  └── Local media storage
Unified relay
  ├── gpt-5.6-sol
  ├── gpt-image-2
  └── doubao-seedance-2-0-fast-260128
```

- API Key 只保存在本地服务端环境变量中。
- 前端不得获得 Key，不得直接调用中转站。
- 供应商响应必须脱敏后才能进入调试日志。

### 14.2 状态存储

SQLite 至少保存：

- 当前书与匿名浏览器标识；
- SafeStoryBrief、模板匹配和冻结故事包的版本与路径；
- 七章插画任务及人工审核状态；
- 七章本地动效任务与技术验收；
- 可选 Seedance 批准、任务和验收状态；
- 当前阅读章节、播放状态和重置状态。

图片、视频和临时文件保存到按 bookId 隔离的本地目录，不存为数据库 BLOB。

### 14.3 建议任务状态

插画：

```text
planned → queued → generating → validating → awaiting_human_review
→ approved | rejected | failed
```

本地动效：

```text
blocked_by_illustration → ready → rendering → validating
→ playable | failed
```

可选增强：

```text
not_requested → awaiting_approval → approved → queued → processing
→ validating → accepted | rejected | failed | stalled
```

整书：

```text
planning → validating_story → generating_illustrations
→ awaiting_illustration_review → rendering_previews
→ ready_to_open → reading → closed
```

---

## 15. 模型与配置

### 15.1 统一接入

- 文本中转基础地址：`https://api.openai-next.com/v1`
- API Key：由本地环境变量统一提供，不进入仓库。
- 固定文本模型：`gpt-5.6-sol`
- 固定图像模型：`gpt-image-2`
- 固定可选视频模型：`doubao-seedance-2-0-fast-260128`
- 模型名称、基础地址和 Key 只允许通过服务端配置读取。

`.env.example` 只包含变量名、已确认地址和模型名，Key 必须为空。

### 15.2 模型准入

开工第一阶段先对中转站做脱敏探针，确认：

- 文本模型可返回可解析的结构化数据；
- 图像模型支持守门 IP 与风格参考输入，并能输出合格竖屏插画；
- 可选视频模型支持以审核插画作为参考或首帧提交、轮询和下载；
- 响应字段、错误格式、任务状态与文件下载方式已记录。

Seedance 探针失败不会阻止默认 2.5D 主链路开工，但必须在界面和验收中保持“可选增强”边界。

---

## 16. P0 功能与验收标准

| P0 功能 | 验收标准 |
|---|---|
| 内耗世界入口 | 无多世界或选卷页面，守门 IP 自然邀请表达 |
| 表达与追问 | 自由文本；最多两次单问题追问；均可跳过 |
| 安全工作流 | 危机路径停止故事与媒体任务且不持久化原文 |
| 内部路由 | 只能得到标签、回声或暂不适配；路由不展示 |
| 七章故事规划 | 第一至第七章都源自本次输入并通过 Schema/语义验收 |
| 风格与插画合同 | 生成 StorybookStyleGuide、ChapterIllustrationPlan 和七张 ChapterCard |
| 七张主插画 | image_selector 并行生成；每章一张；9:16、720×1280 |
| 人工插画审核 | 七张均批准后才能进入对应本地动效 |
| 本地 2.5D 动效 | 七章各 5～8 秒；受控视差与微动；无 3D 和结构变形 |
| 可开书门禁 | 七张插画批准且七章本地预览全部可播放 |
| 绘本阅读 | 像开书一样逐章播放；结束停住；用户主动下一章 |
| 可选增强 | 逐章批准后才调用 Seedance；失败保留本地预览 |
| 刷新恢复 | 恢复故事、审核、媒体任务和阅读进度 |
| 一键重置 | 删除当前书记录与媒体，不影响内置资产 |
| 密钥保护 | Key 不进入前端、仓库、数据库和日志 |

---

## 17. 必测场景

### 17.1 标签卷样例

验证：

- 外界评价或标签真实进入故事机制；
- 七章不是固定事件填空；
- 结尾不是证明评价者错误，而是主角重新取得命名权；
- 七张插画保持同一守门 IP、信物和绘本质感。

### 17.2 回声卷样例

验证：

- 反复确认和担忧放大成为童话因果；
- 故事不承诺“绝不会出错”；
- 结尾允许部分不确定性存在；
- 动效克制，不把焦虑表达成夸张角色变形。

### 17.3 暂不适配样例

验证：

- 不强行进入标签或回声；
- 不提交故事、插画或媒体任务；
- 页面自然邀请用户重新表达或重置。

### 17.4 安全样例

验证：

- 歧义风险进入直接安全确认；
- 明确或无法排除的当前危险进入 `crisis_route`；
- 危机页面不使用童话隐喻；
- 不持久化危机原文，不创建绘本和媒体任务。

### 17.5 失败与恢复样例

至少演练：

- 一个插画任务首次失败后重试成功；
- 一张插画人工拒绝后只重做该章；
- 一个本地动效技术检查失败后重做并通过；
- 一个 Seedance 任务失败后继续保留本地预览；
- 未批准的章节绝不提交 Seedance；
- 五分钟到达但仍缺章时不打开残缺绘本；
- 页面刷新后任务和审核状态继续恢复；
- 一键重置后数据库记录和当前书媒体目录都消失。

---

## 18. Demo 成功标准

### 18.1 产品成功

评委完成体验后能够准确复述：

1. 这不是聊天框，而是一个会把经历写成七章童话的世界；
2. 标签卷和回声卷是系统内部机制，不是用户选择的心理类型；
3. 第一至第七章都根据本次输入生成；
4. 守门 IP 是故事主角，但不是用户替身；
5. 七张审核插画和本地 2.5D 动效构成默认完整绘本；
6. Seedance 只在逐章批准后增强，不影响开书；
7. 用户控制每一章何时开始，故事不会自动播放到底。

### 18.2 工程成功

- 使用真实 `gpt-5.6-sol` 和 `gpt-image-2` 跑通一次完整默认流程。
- 七章 2.5D 预览均由本次输入产生的审核插画制作并可播放。
- 在逐章批准条件下，至少能完成一次 `doubao-seedance-2-0-fast-260128` 探针或增强任务；其失败不影响默认闭环。
- 五分钟内达到可开书状态是 Demo 目标；达不到时能够明确定位未完成章节而不伪造结果。
- 关键状态刷新后可恢复，API Key 不泄露。

### 18.3 视觉成功

- 1920×1080 全屏下无布局溢出、遮挡和原生播放器控件。
- 七章守门 IP 可识别，标志性特征、服装和信物无明显漂移。
- 七张插画保持绘本介质、纸张纹理和统一光色语言。
- 2.5D 动效克制，无廉价 3D、穿模、塑料材质、角色绑定感或明显画面扭曲。
- 章节翻页自然，媒体停止后画面稳定，下一章入口明确但不催促。

---

## 19. 已知风险与开工应对

| 风险 | 开工前或实现中的应对 |
|---|---|
| 中转站请求或响应格式不稳定 | 先做文本、参考图插画和可选视频三类脱敏探针 |
| 文本模型结构化输出不稳定 | 应用层 Schema 校验；一次格式修复；叙事错误完整重规划一次 |
| 七张插画角色或风格漂移 | 固定 IP 参考、StorybookStyleGuide、ChapterIllustrationPlan 与人工审核 |
| 人工审核拖慢现场流程 | 审核界面一次展示七张缩略图与逐章状态，但批准仍按章记录 |
| 2.5D 分层破坏画面 | 限制 2～3 层视差、一个主要镜头运动和两个微动；技术与视觉检查 |
| 五分钟内无法完成默认媒体 | 七张插画和七章本地动效并行；显示可诊断状态；不等待 Seedance |
| Seedance 生成慢或失败 | 逐章批准、后台独立期限、始终保留本地预览 |
| 动画引入新剧情或结构变形 | ChapterCard 禁止项、保留原构图、结果验收，失败不替换 |
| 自动技术检查误判审美 | 自动门禁只管客观可用性；插画由人工审核 |
| 危机资源过期 | 资源包带版本和核验日期；比赛前人工复核官方来源 |
| Demo 本机异常或数据残留 | 启动自检、单书目录隔离、刷新恢复和一键重置演练 |

---

## 20. 未来产品边界（不属于本次验收）

- 更多情绪世界和更多内部故事卷；
- 账户、跨设备同步和用户可见的记忆管理；
- 多本书架、收藏、导出、分享和实体绘本；
- 服务端绘本资产最长三十天留存及生产定时清理；
- 公网部署、多人并发、分布式任务、云对象存储和监控告警；
- 移动端完整体验、无障碍专项验收和商业化能力；
- 更复杂的真实角色动态、跨章视频连续性或默认视频模型链路。

未来扩展不得改变已经确认的基础边界：不诊断、不替用户解释现实、原始倾诉最小化保存、危机路径退出童话。若未来恢复默认真实视频或其他媒体主链路，必须新增 ADR，不得直接重新启用已被取代的旧合同。

---

## 21. 规范性附件

以下文件与本 PRD 共同构成 V0 开工合同；出现冲突时，应先修正文档并形成一致结论，不允许由实现自行选择：

- `CONTEXT.md`：领域词汇；
- `docs/demo-scope-v0.md`：比赛 Demo 范围；
- `docs/model-profile-v0.md`：固定模型与网关；
- `docs/media-profile-v0.md`：插画、2.5D 动效与可选增强合同；
- `docs/generation-sla-v0.md`：开书上限与可选增强后台期限；
- `docs/schemas/*.schema.json`：模型间结构化对象合同；
- `docs/adr/*.md`：架构与产品决策，其中 `superseded` ADR 只保留历史背景。

本 PRD 已完成开工前媒体架构更新；实现应以 V2.1、ADR 0032 和当前 Schema 为准。
